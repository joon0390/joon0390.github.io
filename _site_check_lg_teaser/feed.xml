<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://joon0390.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://joon0390.github.io/" rel="alternate" type="text/html" /><updated>2026-08-20T16:14:00+09:00</updated><id>https://joon0390.github.io/feed.xml</id><title type="html">Heekim’s Blog</title><subtitle>My Blog</subtitle><author><name>Junhee Kim</name></author><entry><title type="html">One-Class Classification</title><link href="https://joon0390.github.io/occ/" rel="alternate" type="text/html" title="One-Class Classification" /><published>2026-05-10T00:00:00+09:00</published><updated>2026-05-10T00:00:00+09:00</updated><id>https://joon0390.github.io/OCC</id><content type="html" xml:base="https://joon0390.github.io/occ/"><![CDATA[<blockquote>
  <p>이 포스팅은 One-Class Classification(OCC) 를 공부하며 정리한 글입니다.</p>
</blockquote>

<hr />

<h1 id="1-one-class-classificationocc이-무엇인가">1. One-Class Classification(OCC)이 무엇인가?</h1>

<p>One-Class Classification(OCC)는 쉽게 말하면 정상 데이터만 보고 정상의 영역을 학습한 뒤, 그 영역 밖의 데이터를 이상치로 판단하는 방법입니다.</p>

<p>일반적인 이진분류 문제에서는 두 클래스가 주어집니다.</p>

\[y \in \{0, 1\}\]

<p>예를 들어 정상 거래와 사기 거래가 모두 충분히 존재하고, 모델은 두 클래스를 구분하는 경계를 학습합니다. 그런데 현실의 이상탐지 문제에서는 비정상 데이터가 거의 없는 경우가 많고, 아예 없는 경우도 존재합니다.</p>

<figure style="text-align:center; margin: 1.8rem 0;">
  <img src="/assets/img/occ/01_class_imbalance.png" alt="Class imbalance" style="width:68%; max-width:720px; display:block; margin: 0 auto;" />

  <figcaption style="font-size:0.9rem; color:#666; margin-top:0.4rem;">이상탐지 문제에서는 정상 데이터에 비해 이상 데이터가 매우 적은 경우가 많습니다.</figcaption>
</figure>

<p>이런 상황에서는 일반적인 이진분류 모델을 사용하기 어렵습니다. 왜냐하면 모델이 비정상 Class의 패턴을 충분하게 학습하기 어렵기 때문입니다.</p>

<p>물론 데이터 불균형을 완화하기 위한 방법론들이 존재하긴 합니다.
예를 들어 소수 클래스를 더 중요하게 학습하도록 손실 함수에 더 큰 가중치를 주는 <strong>class weighting</strong>, 
소수 클래스를 늘리는 <strong>oversampling</strong>, 다수 클래스를 줄이는 <strong>undersampling</strong>, 
또는 오분류 비용을 다르게 설정하는 방법 등이 있습니다.</p>

<figure style="text-align:center; margin: 1.8rem 0;">
  <img src="/assets/img/occ/03_imbalance_remedies_overview.png" alt="Imbalance remedies" style="width:88%; max-width:960px; display:block; margin: 0 auto;" />

  <figcaption style="font-size:0.9rem; color:#666; margin-top:0.4rem;">데이터 불균형을 완화하는 대표적인 접근과 OCC의 차이.</figcaption>
</figure>

<p>하지만 이러한 방법들은 기본적으로 정상 데이터와 이상 데이터가 모두 어느 정도 관측되어 있다는 전제를 가집니다. 
반면 One-Class Classification은 이상 데이터가 매우 적거나, 아예 충분히 확보되지 않은 상황에서 
정상 데이터의 분포 또는 영역을 먼저 학습한다는 점에서 차이가 있다고 볼 수 있습니다.</p>

<hr />

<p>그래서 OCC는 질문을 바꿉니다.</p>

<p>일반적인 이진 분류가 다음과 같은 문제를 푼다면
\(\text{이 데이터는 정상인가, 비정상인가?}\)</p>

<p>OCC는 다음과 같습니다.
\(\text{이 데이터는 정상 데이터의 영역안에 있는가?}\)</p>

<figure style="text-align:center; margin: 1.8rem 0;">
  <img src="/assets/img/occ/02_binary_vs_occ.png" alt="Binary Classification vs OCC" style="width:92%; max-width:980px; display:block; margin: 0 auto;" />

  <figcaption style="font-size:0.9rem; color:#666; margin-top:0.4rem;">이진분류는 두 클래스를 직접 나누지만, OCC는 정상 영역을 먼저 학습합니다.</figcaption>
</figure>

<p>즉, OCC의 핵심은 비정상 Class를 직접 학습하는 것이 아니라, 정상 Class가 차지하는 영역을 학습함으로써, 여집합의 개념으로 비정상을 탐지하는 것입니다.</p>

<blockquote>
  <p>이 글에서는 OCC의 대표적인 방법론으로 OC-SVM과 SVDD를 다루어 보겠습니다.</p>
</blockquote>

<h1 id="2-svmsupport-vector-machine-이란">2. SVM(Support Vector Machine) 이란?</h1>

<p>OC-SVM을 이해하기 위해선 먼저 SVM을 알아야 합니다.</p>

<p>SVM은 기본적으로 이진 분류 모델입니다.</p>

<p>데이터가 다음과 같이 주어진다고 가정해보겠습니다.</p>

\[(x_1, y_1),(x_2,y_2), \dots, (x_n,y_n)\]

<p>여기서 입력 데이터는</p>

\[x_i \in \mathbb R^p\]

<p>이고 라벨은</p>

\[y_i \in \{-1,1\}\]

<p>입니다.</p>

<p>SVM의 목표는 두 Class를 나누는 경계를 찾는 것입니다. 이 경계는 다음과 같은 Hyperplane으로 표현됩니다.</p>

\[w^\top x + b = 0\]

<p>여기서 $w$는 경계의 방향을 결정하는 벡터이고, $b$는 Bias입니다.</p>

<p>새로운 데이터 $x^*$가 들어왔을 때,</p>

\[w^\top x^* + b &gt; 0\]

<p>이면 Class가 1</p>

\[w^\top x^* + b &lt; 0\]

<p>이면 Class를 -1로 분류합니다.</p>

<p>즉 Decision Function은 다음과 같이 쓸 수 있습니다.</p>

\[f(x^*) = sign(w^\top x^* + b)\]

<h1 id="3-svm의-핵심은-margin">3. SVM의 핵심은 Margin</h1>

<p>SVM은 Margin이 가장 큰 경계를 찾습니다.</p>

<p>margin은, 분류 경계와 가장 가까운 데이터 사이의 거리로 정의합니다. SVM은 두 클래스를 나누는 여러 경계 중에서 가장 안정적인 경계를 고르려고 합니다.</p>

<figure style="text-align:center; margin: 1.8rem 0;">
  <img src="/assets/img/occ/04_svm_hard_margin.png" alt="Hard-margin SVM" style="width:78%; max-width:760px; display:block; margin: 0 auto;" />

  <figcaption style="font-size:0.9rem; color:#666; margin-top:0.4rem;">SVM은 여러 가능한 경계 중 margin이 가장 큰 경계를 선택합니다.</figcaption>
</figure>

<p>직관적으로 보면 다음과 같이 생각해볼 수 있습니다.</p>

<table>
  <tbody>
    <tr>
      <td>경계가 데이터에 너무 가까우면, 새로운 데이터가 조금만 흔들려도 오분류될 가능성이 크다. 그러니 Class 사이에 최대한 넓은 여백을 주는 경계를 찾아보자.</td>
    </tr>
  </tbody>
</table>

<p>이 때 분류 경계에 가장 가까이 있는 점들이 중요하며, 이 점들을 Support Vector라고 부릅니다. 
즉, support vector들이 분류 경계를 결정하는 모델입니다.</p>

<h1 id="4-soft-margin-svm">4. Soft-Margin SVM</h1>

<p>현실의 데이터는 완벽하게 나눠지지 않는 경우가 더 많습니다.
그래서 SVM은 어느 정도의 오분류를 허용합니다. 이때 사용하는 변수가 slack variable $\xi_i$ 입니다.</p>

<p>SVM의 soft-margin objective는 다음과 같습니다.</p>

<p>\(\min_{w,b,\xi}\dfrac{1}{2} ||w||^2 +C \sum_{i=1}^n \xi_i\)
subject to
\(y_i(w^\top \phi(x_i) + b) \geq 1- \xi_i, \quad \xi_i \geq 0\)</p>

<table>
  <tbody>
    <tr>
      <td>이 objective에서 첫 번째 항 $\dfrac{1}{2}</td>
      <td> </td>
      <td>w</td>
      <td> </td>
      <td>^2$ 은 Margin을 크게 만들기 위한 항이고 두 번째 항 $C\sum_{i=1}^n\xi_i$ 은 오분류나 margin을 침범한 경우에 대한 penalty입니다.</td>
    </tr>
  </tbody>
</table>

<p>$C$는 두 항의 비율을 조절하는 하이퍼 파라미터입니다. $C$가 크면 오분류에 대한 패널티를 키워서 학습 데이터를 더 강하게 적합하려고 하고, 작아지면 오분류를 어느정도 허용하여 margin을 더 넓게 만들 수 있습니다.</p>

<figure style="text-align:center; margin: 1.8rem 0;">
  <img src="/assets/img/occ/05_svm_soft_margin.png" alt="Soft-margin SVM" style="width:78%; max-width:760px; display:block; margin: 0 auto;" />

  <figcaption style="font-size:0.9rem; color:#666; margin-top:0.4rem;">Soft-margin SVM은 일부 margin violation을 허용하여 현실 데이터에 대응합니다.</figcaption>
</figure>

<h1 id="5-kernel-trick">5. Kernel Trick</h1>
<p>SVM이 강력한 이유 중 하나는 Kernel Trick에 있습니다.</p>

<p>어떤 데이터는 원래 공간에서는 선형적으로 나누기 어렵습니다. 예를 들어 2차원 평면에서는 직선 하나로 클래스를 나누기 어려울 수 있습니다.</p>

<p>이때 데이터를 더 높은 차원의 공간으로 보냅니다.</p>

\[x \rightarrow \phi(x)\]

<p>그러면 기존의 공간에서는 복잡하게 섞여 있던 데이터가 고차원 공간에서는 선형적으로 분리될 수 있습니다. 
하지만 $\phi(x)$를 직접 계산하면 비용이 너무 클 수 있습니다.</p>

<p>그래서 SVM은 $\phi(x)$를 직접 계산하지 않고, 두 점 사이의 내적만 커널 함수를 통해 계산합니다.</p>

\[k(x_i, x_j) = \phi(x_i)^\top \phi(x_j)\]

<p>이게 kernel trick입니다.</p>

<p>SVM의 classification function은 다음과 같이 표현됩니다.</p>

<p>\(f(x) = \sum_{i=1}^n\alpha_iy_i k(x_i,x) + b\)
여기서 $k(x_i,x)$가 kernel function이고, $\alpha_i$는 각 학습 샘플의 중요도를 나타내는 계수입니다.</p>

<figure style="text-align:center; margin: 1.8rem 0;">
  <img src="/assets/img/occ/06_kernel_trick.png" alt="Kernel trick" style="width:92%; max-width:980px; display:block; margin: 0 auto;" />

  <figcaption style="font-size:0.9rem; color:#666; margin-top:0.4rem;">Kernel trick은 원래 공간에서 어려운 분리 문제를 고차원 feature space에서 다루는 아이디어입니다.</figcaption>
</figure>

<h1 id="6-one-class-svm">6. One-Class SVM</h1>
<p>OC-SVM은 일반 SVM을 one-class problem에 맞게 바꾼 모델입니다. 
일반 SVM은 두 class를 나눕니다. 
하지만 OC-SVM은 주로 정상 데이터만 가지고 학습을 진행합니다. 
목표는 다음과 같습니다.</p>

\[\text{정상 데이터가 존재하는 영역을 학습한다.}\]

<p>그리고 새로운 데이터가 그 영역 밖에 존재한다면 anomaly로 판단한다.</p>

<p>OC-SVM의 핵심 아이디어는 정상 데이터와 원점(origin)을 분리하는 Hyperplane을 찾는 것이다.</p>

<p>OC-SVM의 Hyperplane은 다음과 같이 쓸 수 있습니다.</p>

\[w^\top\phi(x) - \rho = 0\]

<p>여기서 $\rho$는 threshold 역할을 합니다.</p>

<p>새로운 데이터 $x^*$에 대해서</p>

\[w^\top\phi(x^*) - \rho \geq 0\]

<p>이면 정상</p>

\[w^\top\phi(x^*) - \rho \leq 0\]

<p>이면 anomaly로 분류하는 것입니다.</p>

<figure style="text-align:center; margin: 1.8rem 0;">
  <img src="/assets/img/occ/08_ocsvm_basic_boundary.png" alt="OC-SVM boundary" style="width:78%; max-width:760px; display:block; margin: 0 auto;" />

  <figcaption style="font-size:0.9rem; color:#666; margin-top:0.4rem;">OC-SVM은 정상 데이터가 차지하는 영역을 학습하고, 그 밖의 점을 anomaly로 봅니다.</figcaption>
</figure>

<figure style="text-align:center; margin: 1.8rem 0;">
  <img src="/assets/img/occ/11_ocsvm_origin_hyperplane_concept.png" alt="OC-SVM origin hyperplane concept" style="width:78%; max-width:760px; display:block; margin: 0 auto;" />

  <figcaption style="font-size:0.9rem; color:#666; margin-top:0.4rem;">Feature space에서 정상 데이터와 origin을 분리하는 OC-SVM의 기하학적 관점.</figcaption>
</figure>

<h1 id="7-oc-svm의-목적함수">7. OC-SVM의 목적함수</h1>

<p>OC-SVM의 최적화 문제는 다음과 같습니다.
\(\min_{w,\rho,\xi} \dfrac{1}{2}||w||^2 + \dfrac{1}{\nu n}\sum_{i=1}^n \xi_i -\rho\)
subject to</p>

\[w^\top\phi(x_i)\geq \rho - \xi_i,\quad \xi_i \geq 0\]

<p>이 식을 하나씩 뜯어보면 다음과 같습니다.</p>

<p>첫 번째 항은 margin을 조절하고, 두 번째 항은 boundary를 위반한 데이터에 대한 penalty로 해석할 수 있습니다.
마지막 세 번째 항은 $\rho$를 크게 하도록 유도합니다.</p>

<blockquote>
  <p>즉 OC-SVM은 정상 데이터들을 origin으로부터 잘 분리하면서, 동시에 너무 많은 데이터를 boundary 밖으로 보내지 않도록 학습하게 됩니다.</p>
</blockquote>

<h1 id="8-nu-는-무엇인가">8. $\nu$ 는 무엇인가</h1>

<p>OC-SVM에서 $\nu$는 매우 중요한 파라미터입니다.
$\nu$는 두 가지 의미를 가집니다.</p>

<ol>
  <li>Outlier 비율의 upper bound 역할</li>
  <li>Support Vector 비율의 lower bound 역할</li>
</ol>

<p>쉽게 말하면 $\nu$는 모델에게 이런 식으로 말하는 것입니다.</p>

<table>
  <tbody>
    <tr>
      <td>정상 데이터 중 일부는 boundary 밖에 있어도 괜찮아. 그 허용 정도를 $\nu$로 하자!</td>
    </tr>
  </tbody>
</table>

<p>$\nu$가 작으면 두 번째 항이 커지게 되고, 그래서 대부분의 데이터를 정상 영역안에 포함시키려고 합니다. 그래서 결과적으로 정상 영역이 넓어지게 됩니다.</p>

<p>반대로 $\nu$가 크면 패널티가 작아져, 일부 데이터를 boundary 밖으로 보내는 것을 허용하게 되고, 결과적으로 boundary가 더 빡빡해지게 됩니다.</p>

<figure style="text-align:center; margin: 1.8rem 0;">
  <img src="/assets/img/occ/10_ocsvm_nu_effect.png" alt="OC-SVM nu effect" style="width:96%; max-width:1050px; display:block; margin: 0 auto;" />

  <figcaption style="font-size:0.9rem; color:#666; margin-top:0.4rem;">$\nu$ 값에 따라 정상 영역의 넓이와 boundary의 tightness가 달라집니다.</figcaption>
</figure>

<h1 id="9-oc-svm의-decision-function">9. OC-SVM의 Decision function</h1>

<p>OC-SVM의 학습이 끝나면 새로운 데이터 $x^*$에 대해서 다음 값을 계산합니다.</p>

\[f(x^*) = sign\bigg(\sum_{i=1}^n\alpha_i^* k(x_i, x^*)-\rho^*\bigg)\]

<p>여기서 $\alpha_i^<em>, \rho^</em>$는 학습된 계수 값이고 $k$는 커널 함수입니다.</p>

<p>이 결과가 1이면 정상, -1이면 비정상으로 판단하는 것입니다.</p>

<blockquote>
  <p>즉, OC-SVM은 새로운 데이터가 support vector들과 얼마나 비슷한 지를 보고, 그 값이 threshold $\rho^*$보다 충분히 큰 지 확인합니다.</p>
</blockquote>

<h1 id="10-svdd-란">10. SVDD 란?</h1>
<p>SVDD는 Support Vector Data Description의 약자입니다.</p>

<blockquote>
  <p>OC-SVM이 정상 데이터와 origin을 분리하는 Hyperplane을 찾는 방법이라면, SVDD는 정상 데이터를 감싸는 가장 작은 Hypersphere를 찾는 방법이라고 할 수 있습니다.</p>
</blockquote>

<p>즉, OC-SVM의 개념은 다음과 같습니다.</p>

\[\text{정상 데이터 vs Origin}\]

<p>반면 SVDD는</p>

\[\text{정상 데이터를 감싸는 최소의 초구}\]

<p>SVDD의 목표는 Hypersphere의 중심 $a$와 반지름 $R$을 찾는 것입니다.</p>

<p>\(\min_{R,a,\xi} R^2 + C\sum_{i=1}^n \xi_i\)
subject to 
\(||\phi(x_i) - a||^2 \leq R^2 + \xi_i,\quad \xi_i \geq 0\)</p>

<p>정상 데이터는 이 sphere 내부에 있어야 합니다.</p>

<p>그래서 새로운 데이터가 들어오면</p>

\[||\phi(x^*) - a||^2 \leq R^2\]

<p>이면 정상, 그렇지 않으면 비정상으로 분류합니다.</p>

<figure style="text-align:center; margin: 1.8rem 0;">
  <img src="/assets/img/occ/12_svdd_hypersphere.png" alt="SVDD hypersphere" style="width:78%; max-width:760px; display:block; margin: 0 auto;" />

  <figcaption style="font-size:0.9rem; color:#666; margin-top:0.4rem;">SVDD는 정상 데이터를 감싸는 최소 부피의 hypersphere를 찾는 방식입니다.</figcaption>
</figure>

<h1 id="11-deep-svdd-란">11. Deep SVDD 란?</h1>
<p>마지막으로 Deep SVDD라는 SVDD에 deep learning을 결합한 방법을 간략히 살펴보겠습니다.</p>

<p>기존 SVDD에서는 Feature mapping을 커널 트릭 $\phi(x)$를 사용하여 표현합니다.</p>

<p>하지만 Deep SVDD는 이 feature mapping을 neural network로 바꿉니다.</p>

\[\phi(x;W)\]

<p>여기서 $W$는 신경망의 가중치입니다.</p>

<blockquote>
  <p>즉, Deep SVDD는 raw input을 신경망에 넣어서 representation을 만들고, 그 representation들이 하나의 중심 근처에 모이도록 학습을 진행합니다.</p>
</blockquote>

<p>objective는 다음과 같습니다.</p>

\[\min_W\dfrac{1}{n}\sum_{i=1}^n||\phi(x_i;W)||^2 + \dfrac{\lambda}{2}\sum_{h=1}^H ||W_h||^2\]

<figure style="text-align:center; margin: 1.8rem 0;">
  <img src="/assets/img/occ/14_deep_svdd_pipeline.png" alt="Deep SVDD pipeline" style="width:94%; max-width:1000px; display:block; margin: 0 auto;" />

  <figcaption style="font-size:0.9rem; color:#666; margin-top:0.4rem;">Deep SVDD는 neural network로 feature representation을 학습한 뒤, 정상 데이터를 중심 근처에 모읍니다.</figcaption>
</figure>

<h1 id="12-oc-svm-vs-svdd">12. OC-SVM vs SVDD</h1>

<p>OC-SVM과 SVDD는 둘다 OCC 방법론이지만, 정상 영역을 정의하는 방식이 다릅니다.</p>

<table>
  <thead>
    <tr>
      <th>모델</th>
      <th>핵심 아이디어</th>
      <th>기하학적 관점</th>
      <th>이상치 판단</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>OC-SVM</td>
      <td>정상 데이터와 origin을 분리</td>
      <td>Hyperplane</td>
      <td>경계의 바깥이면 이상치로 분류</td>
    </tr>
    <tr>
      <td>SVDD</td>
      <td>정상 데이터를 sphere로 감쌈</td>
      <td>Hypersphere</td>
      <td>Hypersphere의 바깥이면 이상치로 분류</td>
    </tr>
    <tr>
      <td>Deep SVDD</td>
      <td>neural network representation을 Hypersphere 안에 모음</td>
      <td>learned feature space + hypersphere</td>
      <td>중심에서 멀면 이상치로 분류</td>
    </tr>
  </tbody>
</table>

<figure style="text-align:center; margin: 1.8rem 0;">
  <img src="/assets/img/occ/13_ocsvm_vs_svdd.png" alt="OC-SVM vs SVDD" style="width:92%; max-width:980px; display:block; margin: 0 auto;" />

  <figcaption style="font-size:0.9rem; color:#666; margin-top:0.4rem;">OC-SVM은 hyperplane 관점, SVDD는 hypersphere 관점으로 정상 영역을 정의합니다.</figcaption>
</figure>

<h1 id="conclusion">Conclusion</h1>

<p>오늘은 One-Class Classification의 개념과 대표적인 방법론인 OC-SVM, SVDD, Deep SVDD에 대해 정리해보았습니다.</p>

<p>일반적인 이진분류는 정상 데이터와 비정상 데이터가 모두 어느 정도 존재한다는 전제에서 두 클래스를 나누는 경계를 학습합니다. 반면 OCC는 비정상 데이터가 매우 적거나 충분하지 않은 상황에서 정상 데이터가 차지하는 영역을 먼저 학습하고, 그 영역의 바깥을 비정상으로 판단합니다.</p>

<p>SVM은 두 클래스를 나누는 여러 경계 중 margin이 가장 큰 경계를 찾는 모델입니다. OC-SVM은 이 아이디어를 one-class setting으로 확장하여, feature space에서 정상 데이터와 origin을 분리하는 경계를 학습합니다. 이때 $\nu$는 허용할 outlier의 비율과 support vector의 비율에 영향을 주기 때문에, 정상 영역을 얼마나 넓게 또는 빡빡하게 잡을지를 결정하는 중요한 하이퍼파라미터입니다.</p>

<p>SVDD는 OC-SVM과는 조금 다른 관점에서 정상 데이터를 설명합니다. OC-SVM이 hyperplane을 이용해 정상 영역을 구분한다면, SVDD는 정상 데이터를 감싸는 최소 크기의 hypersphere를 찾습니다. Deep SVDD는 여기에 neural network를 결합하여, 원본 데이터가 아니라 신경망이 학습한 representation space에서 정상 데이터를 하나의 중심 근처에 모으는 방식입니다.</p>

<p>결국 OCC의 핵심은 “비정상을 직접 학습한다”기보다는 “정상이 무엇인지 먼저 정의한다”는 데 있습니다. 따라서 이상 데이터의 수가 매우 적은 문제, 새로운 유형의 이상이 계속 등장하는 문제, 또는 비정상 패턴을 사전에 충분히 수집하기 어려운 문제에서 유용하게 사용할 수 있습니다. 다만 정상과 이상이 많이 겹치는 경우, 정상 데이터 자체가 매우 다양하게 분포하는 경우, 또는 하이퍼파라미터를 부적절하게 선택하는 경우에는 성능이 크게 달라질 수 있습니다. 실제 적용에서는 데이터의 특성, 이상 탐지의 목적, false positive와 false negative의 비용을 함께 고려해야 합니다.</p>

<h1 id="references">References</h1>

<ul>
  <li>Cortes, C., &amp; Vapnik, V. (1995). <a href="https://doi.org/10.1007/BF00994018">Support-vector networks</a>. <em>Machine Learning</em>, 20, 273–297.</li>
  <li>Boser, B. E., Guyon, I. M., &amp; Vapnik, V. N. (1992). <a href="https://doi.org/10.1145/130385.130401">A training algorithm for optimal margin classifiers</a>. <em>Proceedings of the Fifth Annual Workshop on Computational Learning Theory</em>, 144–152.</li>
  <li>Schölkopf, B., Platt, J. C., Shawe-Taylor, J., Smola, A. J., &amp; Williamson, R. C. (2001). <a href="https://doi.org/10.1162/089976601750264965">Estimating the support of a high-dimensional distribution</a>. <em>Neural Computation</em>, 13(7), 1443–1471.</li>
  <li>Tax, D. M. J., &amp; Duin, R. P. W. (2004). <a href="https://doi.org/10.1023/B:MACH.0000008084.60811.49">Support Vector Data Description</a>. <em>Machine Learning</em>, 54, 45–66.</li>
  <li>Ruff, L., Vandermeulen, R., Görnitz, N., Deecke, L., Siddiqui, S. A., Binder, A., Müller, E., &amp; Kloft, M. (2018). <a href="https://proceedings.mlr.press/v80/ruff18a.html">Deep One-Class Classification</a>. <em>Proceedings of the 35th International Conference on Machine Learning</em>, PMLR 80, 4393–4402.</li>
  <li>Perera, P., Oza, P., &amp; Patel, V. M. (2021). <a href="https://arxiv.org/abs/2101.03064">One-Class Classification: A Survey</a>. <em>arXiv:2101.03064</em>.</li>
  <li>He, H., &amp; Garcia, E. A. (2009). <a href="https://doi.org/10.1109/TKDE.2008.239">Learning from Imbalanced Data</a>. <em>IEEE Transactions on Knowledge and Data Engineering</em>, 21(9), 1263–1284.</li>
  <li>Chawla, N. V., Bowyer, K. W., Hall, L. O., &amp; Kegelmeyer, W. P. (2002). <a href="https://doi.org/10.1613/jair.953">SMOTE: Synthetic Minority Over-sampling Technique</a>. <em>Journal of Artificial Intelligence Research</em>, 16, 321–357.</li>
</ul>]]></content><author><name>Junhee Kim</name></author><category term="Machine Learning" /><category term="Deep Learning" /><category term="Statistics" /><category term="Classification" /><category term="One-Class Classification" /><category term="Anomaly Detection" /><summary type="html"><![CDATA[이 포스팅은 One-Class Classification(OCC) 를 공부하며 정리한 글입니다.]]></summary></entry><entry><title type="html">DiT : Scalable Diffusion Models with Transformers</title><link href="https://joon0390.github.io/dit/" rel="alternate" type="text/html" title="DiT : Scalable Diffusion Models with Transformers" /><published>2026-04-10T00:00:00+09:00</published><updated>2026-04-10T00:00:00+09:00</updated><id>https://joon0390.github.io/DiT</id><content type="html" xml:base="https://joon0390.github.io/dit/"><![CDATA[<blockquote>
  <p>이 포스팅은 DiT (Diffusion Transformer)를 공부하며<br />
통계학적인 관점에서 핵심 개념을 정리한 글입니다.</p>
</blockquote>

<hr />

<h1 id="1-introduction">1. Introduction</h1>

<p>최근 디퓨전 모델은 이미지 생성 분야에서 중요한 역할을 하는 생성 모델로 자리 잡았습니다.<br />
특히 <a href="https://arxiv.org/pdf/2006.11239">DDPM (Ho et al., 2020)</a> 이후,<br />
노이즈를 점진적으로 제거하는 방식의 생성 모델이 높은 성능을 보이며 빠르게 발전했습니다.</p>

<p>이후 <a href="https://arxiv.org/pdf/2010.02502">DDIM (Song et al., 2021)</a>은<br />
샘플링 방식을 개선하여 더 빠른 생성이 가능하도록 하였으며,</p>

<p><a href="https://arxiv.org/pdf/2112.10752">Latent Diffusion Models (Rombach et al., 2022)</a>는<br />
이미지를 압축된 latent 공간에서 처리함으로써 계산 효율을 크게 개선하였고,<br />
스테이블 디퓨전과 같은 실제 서비스 모델의 기반이 되었습니다.</p>

<p>이러한 흐름 속에서 등장한 <a href="https://arxiv.org/pdf/2212.09748">DiT (Peebles &amp; Xie, 2023)</a>는<br />
기존 diffusion 모델의 구조를 크게 바꾸기보다는,<br />
<strong>backbone을 CNN(U-Net)에서 Transformer로 확장한 모델</strong>입니다.</p>

<hr />

<h2 id="dit를-어떻게-보면-좋을까">DiT를 어떻게 보면 좋을까?</h2>

<p>DiT는 완전히 새로운 diffusion 모델이라기보다는,<br />
다음과 같이 이해하는 것이 자연스럽습니다.</p>

<ul>
  <li>diffusion의 기본 구조는 그대로 유지됩니다</li>
  <li>학습 방식 (noise prediction)도 동일합니다</li>
  <li>단지 이를 구현하는 모델을 Transformer로 변경합니다</li>
</ul>

<p>즉,</p>

\[\text{DiT} = \text{Diffusion Model} + \text{Transformer}\]

<p>라고 볼 수 있습니다.</p>

<hr />

<h2 id="이-글에서-볼-내용">이 글에서 볼 내용</h2>

<p>이 글에서는 DiT를 이해하기 위해 다음 흐름으로 정리하였습니다.</p>

<ul>
  <li>diffusion model의 기본 구조</li>
  <li>DDPM과 DDIM의 차이</li>
  <li>DiT가 기존 구조에서 무엇을 바꿨는지</li>
</ul>

<hr />

<h1 id="2-diffusion-model-기본-구조">2. Diffusion Model 기본 구조</h1>

<p>Diffusion model은 데이터를 점진적으로 노이즈화한 뒤,<br />
이를 다시 복원하는 방식의 생성 모델입니다.</p>

<p><img src="/assets/img/dit/image_1.png" alt="Diffusion process" /></p>

<p><em>Figure 1. Diffusion model의 forward(노이즈 추가)와 reverse(복원) 과정</em></p>

<p>위 그림(Figure 1)과 같이 diffusion 모델은 데이터를 점진적으로 노이즈화한 뒤,<br />
이를 다시 복원하는 구조를 가집니다.</p>

<hr />

<h2 id="21-forward-process">2.1 Forward Process</h2>

\[x_t = \sqrt{\bar\alpha_t} x_0 + \sqrt{1-\bar\alpha_t} \epsilon\]

<p>이 식은 시간 $ t $에서의 데이터 $ x_t $가<br />
원본 데이터 $ x_0 $와 Gaussian noise의 조합으로 표현된다는 것을 의미합니다.</p>

<ul>
  <li>$ x_0 $: 원본 데이터</li>
  <li>$ \epsilon \sim \mathcal{N}(0, I) $: Gaussian noise</li>
  <li>$ \bar{\alpha}<em>t = \prod</em>{s=1}^{t}(1 - \beta_s) $: 누적 계수</li>
</ul>

<p>즉,</p>

<ul>
  <li>원본 데이터에 점점 더 많은 노이즈가 섞이게 되고</li>
  <li>시간이 지날수록 데이터는 점점 무작위 노이즈에 가까워집니다</li>
</ul>

<hr />

<h1 id="3-ddpm-vs-ddim">3. DDPM vs DDIM</h1>

<p>Diffusion 모델에서는<br />
<strong>학습과 샘플링을 구분해서 이해하는 것이 중요합니다.</strong></p>

<p><img src="/assets/img/dit/ddpm_ddim.jpg" alt="DDPM vs DDIM" /></p>

<p><em>Figure 2. DDPM의 stochastic sampling과 DDIM의 deterministic trajectory 비교</em></p>

<p>Figure 2에서 볼 수 있듯이, DDPM은 stochastic한 경로를 따르는 반면<br />
DDIM은 deterministic한 trajectory를 사용합니다.</p>

<hr />

<h1 id="4-dit-diffusion-transformer">4. DiT (Diffusion Transformer)</h1>

<p>이제 핵심인 DiT를 살펴보겠습니다.</p>

<hr />

<h2 id="42-구조-개요">4.2 구조 개요</h2>

<p>DiT는 다음과 같은 과정을 거칩니다.</p>

<ol>
  <li>이미지 → patch로 분할</li>
  <li>patch → token으로 변환</li>
  <li>Transformer 적용</li>
  <li>noise prediction 출력</li>
</ol>

<p><img src="/assets/img/dit/dit.jpg" alt="DiT architecture" /></p>

<p><em>Figure 3. DiT 구조: 이미지 → patch → Transformer → noise prediction</em></p>

<p>Figure 3과 같이 DiT는 이미지를 patch 단위로 분할한 뒤,<br />
Transformer를 통해 전역 정보를 학습합니다.</p>

<hr />

<h2 id="43-self-attention">4.3 Self-Attention</h2>

\[\text{Attention}(Q, K, V)
=
\text{softmax}\left(\frac{QK^T}{\sqrt{d}}\right)V\]

<p>이 구조 덕분에</p>

<ul>
  <li>이미지 전체 정보를 동시에 고려할 수 있으며</li>
  <li>장거리 의존성을 효과적으로 학습할 수 있습니다</li>
</ul>

<p><img src="/assets/img/dit/self_attention.jpg" alt="Self Attention" /></p>

<p><em>Figure 4. Self-attention을 통해 patch 간 관계를 학습하는 방식</em></p>

<p>Figure 4와 같이 self-attention은 각 patch 간의 관계를 가중치 형태로 학습합니다.</p>

<hr />

<h2 id="44-왜-transformer인가">4.4 왜 Transformer인가?</h2>

<p>기존 CNN과 비교하면 다음과 같습니다.</p>

<ul>
  <li>CNN → 지역 정보에 강함</li>
  <li>Transformer → 전체 구조 이해에 강함</li>
</ul>

<p>특히,</p>

<ul>
  <li>모델을 크게 확장할수록 성능이 잘 증가하며</li>
  <li>text conditioning과의 궁합이 좋습니다</li>
</ul>

<hr />

<h1 id="5-dit-vs-u-net">5. DiT vs U-Net</h1>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>U-Net</th>
      <th>DiT</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>구조</td>
      <td>CNN</td>
      <td>Transformer</td>
    </tr>
    <tr>
      <td>정보 처리</td>
      <td>local</td>
      <td>global</td>
    </tr>
    <tr>
      <td>확장성</td>
      <td>제한적</td>
      <td>우수</td>
    </tr>
    <tr>
      <td>계산 비용</td>
      <td>비교적 낮음</td>
      <td>높음</td>
    </tr>
  </tbody>
</table>

<hr />

<h1 id="6-전체-흐름-정리">6. 전체 흐름 정리</h1>

<p>Diffusion 모델의 핵심 구조는 다음과 같습니다.</p>

\[x_t = \sqrt{\bar\alpha_t} x_0 + \sqrt{1-\bar\alpha_t}\epsilon\]

<p>모델은 다음을 학습합니다.</p>

\[\epsilon_\theta(x_t, t)\]

<hr />

<h2 id="구성-요소-정리">구성 요소 정리</h2>

<ul>
  <li><strong>DDPM</strong>: 기본 diffusion 구조 + stochastic sampling</li>
  <li><strong>DDIM</strong>: 같은 모델, 더 빠른 sampling</li>
  <li><strong>DiT</strong>: Transformer 기반 noise predictor</li>
</ul>

<hr />

<p>즉,</p>

<ul>
  <li>DDPM → 어떻게 학습하고 생성할 것인가</li>
  <li>DDIM → 어떻게 빠르게 생성할 것인가</li>
  <li>DiT → 어떤 모델로 학습할 것인가</li>
</ul>

<hr />

<h1 id="7-개인-정리-insight">7. 개인 정리 (Insight)</h1>

<p>DiT를 공부하면서 다음과 같은 점이 중요하다고 느꼈습니다.</p>

<hr />

<h2 id="1-diffusion은-구조적으로-단순합니다">1. diffusion은 구조적으로 단순합니다</h2>

<ul>
  <li>결국 noise를 맞추는 문제입니다</li>
  <li>하지만 매우 강력한 생성 모델입니다</li>
</ul>

<hr />

<h2 id="2-핵심은-함수-근사입니다">2. 핵심은 함수 근사입니다</h2>

\[\epsilon_\theta(x_t, t)\]

<p>이 함수가 얼마나 복잡한 분포를 잘 근사하는지가 중요합니다.</p>

<hr />

<h2 id="3-transformer는-이-문제에-잘-맞습니다">3. Transformer는 이 문제에 잘 맞습니다</h2>

<ul>
  <li>global dependency</li>
  <li>scaling 성능</li>
  <li>조건부 생성과의 궁합</li>
</ul>

<hr />

<p>즉 DiT의 핵심은</p>

<blockquote>
  <p>diffusion 구조의 변화가 아니라 함수 클래스의 변화입니다.</p>
</blockquote>

<hr />

<h1 id="8-마무리">8. 마무리</h1>

<p>DiT는 diffusion model의 새로운 이론이라기보다는,<br />
기존 구조 위에서 Transformer를 적용한 모델입니다.</p>

<p>하지만 이 변화는 단순한 구조 변경을 넘어,</p>

<ul>
  <li>성능 향상</li>
  <li>확장성 증가</li>
  <li>새로운 연구 방향</li>
</ul>

<p>을 제시했다는 점에서 의미가 있습니다.</p>

<hr />

<h1 id="reference">Reference</h1>

<ul>
  <li>Ho, J., Jain, A., &amp; Abbeel, P. (2020). <em>Denoising Diffusion Probabilistic Models</em></li>
  <li>Song, J., Meng, C., &amp; Ermon, S. (2021). <em>Denoising Diffusion Implicit Models</em></li>
  <li>Rombach, R. et al. (2022). <em>Latent Diffusion Models</em></li>
  <li>Peebles, W., &amp; Xie, S. (2023). <em>Scalable Diffusion Models with Transformers</em></li>
</ul>]]></content><author><name>Junhee Kim</name></author><category term="Deep Learning" /><category term="Applied Topics" /><category term="Statistics" /><category term="Diffusion Transformer" /><category term="Generative Model" /><category term="Image Generation" /><summary type="html"><![CDATA[이 포스팅은 DiT (Diffusion Transformer)를 공부하며 통계학적인 관점에서 핵심 개념을 정리한 글입니다.]]></summary></entry><entry><title type="html">[Paper Review] Predictive variational inference: Learn the predictively optimal posterior distribution</title><link href="https://joon0390.github.io/predictive-variational-inference/" rel="alternate" type="text/html" title="[Paper Review] Predictive variational inference: Learn the predictively optimal posterior distribution" /><published>2026-04-06T00:00:00+09:00</published><updated>2026-04-06T00:00:00+09:00</updated><id>https://joon0390.github.io/PVI</id><content type="html" xml:base="https://joon0390.github.io/predictive-variational-inference/"><![CDATA[<blockquote>
  <p>이 포스팅은 Lai, Linero, Yao 의 2024년 arXiv 논문<br />
<a href="https://arxiv.org/abs/2410.14843">“Predictive variational inference: Learn the predictively optimal posterior distribution”</a><br />
를 읽고 정리한 글입니다. 이 글에서 참고한 버전은 <strong>2026년 3월 30일 공개된 v3</strong> 입니다.</p>
</blockquote>

<p>이전에 정리한 <a href="/variational-inference/"><strong>Variational Inference</strong></a> 와<br />
<a href="/generalized-variational-inference/"><strong>Generalized Variational Inference</strong></a> 를 먼저 보고 오면 더 잘 연결됩니다.</p>

<hr />

<h2 id="introduction">Introduction</h2>

<p>이 논문의 핵심 메시지는 아주 명확합니다.</p>

<blockquote>
  <p><strong>모델이 틀릴 수 있다면, exact Bayes posterior 를 잘 근사하는 것 자체가 목표가 아닐 수 있다.</strong></p>
</blockquote>

<p>기존의 VI는 보통</p>

\[q^\star(\theta)=
\arg\min_{q\in\mathcal F}
\mathrm{KL}\big(q(\theta)\,\|\,p(\theta\mid y)\big)\]

<p>를 풀어서 <strong>Bayesian posterior</strong> 를 근사합니다.</p>

<p>하지만 논문 저자들은 문제를 제기합니다.</p>

<ul>
  <li>우리가 정말 원하는 것이 parameter recovery 인가?</li>
  <li>아니면 <strong>좋은 posterior predictive distribution</strong> 인가?</li>
  <li>그리고 모델이 misspecified 되어 있다면, exact posterior 자체가 이미 잘못된 target 아닌가?</li>
</ul>

<p><strong>Predictive Variational Inference (PVI)</strong> 는 이 질문에서 시작합니다.</p>

<p>PVI는 posterior 자체를 맞추는 대신,</p>

\[q_\phi^Y(\tilde y)=
\int_\Theta p(\tilde y\mid \theta)\,q_\phi(\theta)\,d\theta\]

<p>라는 <strong>posterior predictive distribution</strong> 이
실제 데이터 생성 과정에 최대한 가깝도록 하는 $q_\phi(\theta)$ 를 학습합니다.</p>

<p>즉, VI가</p>

<blockquote>
  <p>“posterior approximation”</p>
</blockquote>

<p>에 가깝다면,</p>

<p>PVI는</p>

<blockquote>
  <p>“predictive distribution optimization”</p>
</blockquote>

<p>에 가깝습니다.</p>

<hr />

<h2 id="standard-vi-와-뭐가-다른가">Standard VI 와 뭐가 다른가?</h2>

<h3 id="vi는-posterior를-pvi는-predictive를-target으로-삼는다">VI는 posterior를, PVI는 predictive를 target으로 삼는다</h3>

<p>기존 VI의 ELBO는 다음과 같습니다.</p>

\[\mathrm{ELBO}(q)=
\mathbb E_q\left[\sum_{i=1}^n \log p(y_i\mid\theta)\right]
-\mathrm{KL}\big(q(\theta)\,\|\,p^{\mathrm{prior}}(\theta)\big).\]

<p>반면 PVI는 적절한 scoring rule $S$ 를 정한 뒤,</p>

\[\phi^\star=
\arg\max_{\phi\in\Phi}
\left\{
\sum_{i=1}^n
S\big(q_\phi^Y, y_i\big)-
\lambda r(\phi)
\right\}\]

<p>를 풉니다.</p>

<p>여기서 핵심은 <strong>어디에 로그가 붙는가</strong> 입니다.</p>

<ul>
  <li>
    <p>VI:</p>

\[\mathbb E_q[\log p(y_i\mid\theta)]\]
  </li>
  <li>
    <p>PVI의 log-score 버전:</p>

\[\log \int p(y_i\mid\theta)\,q_\phi(\theta)\,d\theta\]
  </li>
</ul>

<p>즉,</p>

<ul>
  <li>VI는 <strong>posterior 평균을 낸 local likelihood fit</strong></li>
  <li>PVI는 <strong>mixing 후의 predictive distribution fit</strong></li>
</ul>

<p>을 본다는 점에서 다릅니다.</p>

<p>이 차이는 단순한 수식 위치 차이가 아니라,
<strong>무엇을 최적화하는지 자체가 다르다</strong>는 뜻입니다.</p>

<table>
  <thead>
    <tr>
      <th>관점</th>
      <th>Standard VI</th>
      <th>PVI</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>목표</td>
      <td>$p(\theta\mid y)$ 근사</td>
      <td>$q_\phi^Y(\tilde y)$ 최적화</td>
    </tr>
    <tr>
      <td>공간</td>
      <td>parameter space</td>
      <td>outcome space</td>
    </tr>
    <tr>
      <td>기준</td>
      <td>posterior divergence / ELBO</td>
      <td>scoring rule 기반 predictive fit</td>
    </tr>
    <tr>
      <td>misspecification 에서</td>
      <td>정확한 posterior를 잘 근사해도 predictive가 나쁠 수 있음</td>
      <td>predictive quality 자체를 바로 target으로 삼음</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="proper-scoring-rule-로-predictive-optimality-정의하기">Proper Scoring Rule 로 predictive optimality 정의하기</h2>

<p>PVI는 “좋은 예측”을 <strong>proper scoring rule</strong> 로 정의합니다.</p>

<p>논문에서 쓰는 기본 아이디어는 다음과 같습니다.</p>

<p>확률적 예측 분포 $P$ 에 대해 score 를 $S(P,\tilde y)$ 라고 하면,
proper scoring rule 은 참분포 $p_{\mathrm{true}}$ 에서 최대가 되는 score 입니다.</p>

<p>이를 이용하면</p>

\[D_S(P, p_{\mathrm{true}})=
S(p_{\mathrm{true}}, p_{\mathrm{true}})-
S(P, p_{\mathrm{true}})\]

<p>처럼 <strong>outcome space 상의 divergence</strong> 를 정의할 수 있습니다.</p>

<p>즉 PVI는</p>

<blockquote>
  <p>parameter space 에서 posterior를 맞추는 대신,<br />
outcome space 에서 predictive distribution을 맞춥니다.</p>
</blockquote>

<p>논문에서 다루는 대표적인 score 는 다음 네 가지입니다.</p>

<h3 id="1-logarithmic-score">1. Logarithmic score</h3>

\[S(P, y)=\log P(y)\]

<ul>
  <li>가장 표준적인 score 입니다.</li>
  <li>이 경우 PVI는 predictive log-likelihood 를 직접 최적화합니다.</li>
  <li>KL 관점과 연결되지만, <strong>Bayes posterior approximation</strong> 과는 여전히 다릅니다.</li>
</ul>

<h3 id="2-quadratic-score--brier-score">2. Quadratic score / Brier score</h3>

<ul>
  <li>categorical outcome 에 적합합니다.</li>
  <li>log score 보다 calibration 차이를 더 민감하게 볼 수 있습니다.</li>
</ul>

<h3 id="3-interval-score">3. Interval score</h3>

<ul>
  <li>prediction interval 의 <strong>sharpness + coverage</strong> 를 함께 봅니다.</li>
  <li>예측구간이 좁으면서도 데이터가 그 안에 잘 들어오는지를 평가합니다.</li>
</ul>

<h3 id="4-crps">4. CRPS</h3>

<ul>
  <li>예측분포 전체의 calibration 을 보는 score 입니다.</li>
  <li>논문에서 특히 중요한 이유는,
<strong>likelihood 값을 계산하지 못해도 posterior predictive sample 만 있으면 쓸 수 있기 때문</strong>입니다.</li>
  <li>그래서 PVI는 <strong>simulation-based inference (SBI)</strong> 나 likelihood-free setting 에도 바로 연결됩니다.</li>
</ul>

<hr />

<h2 id="regularization-prior-쪽으로-붙일지-posterior-쪽으로-붙일지">Regularization: prior 쪽으로 붙일지, posterior 쪽으로 붙일지</h2>

<p>논문은 PVI objective 에 regularization 항을 넣습니다.</p>

<p>두 가지 선택이 있습니다.</p>

\[r^{\mathrm{prior}}(\phi)=
\mathrm{KL}\big(q_\phi(\theta)\,\|\,p^{\mathrm{prior}}(\theta)\big)\]

\[r^{\mathrm{post}}(\phi)=
\mathrm{KL}\big(q_\phi(\theta)\,\|\,p(\theta\mid y)\big)\]

<p>해석은 다음과 같습니다.</p>

<ol>
  <li><strong>Prior regularization</strong>
    <ul>
      <li>standard VI 에서 보던 prior regularization 과 비슷합니다.</li>
      <li>predictive fit 을 보되, prior 에서 너무 멀어지지 않게 합니다.</li>
    </ul>
  </li>
  <li><strong>Posterior regularization</strong>
    <ul>
      <li>exact posterior 쪽으로 당기는 regularization 입니다.</li>
      <li>$\lambda$ 를 키우면 PVI가 점점 Bayes/VI 쪽으로 가까워집니다.</li>
    </ul>
  </li>
</ol>

<p>즉 PVI는</p>

<ul>
  <li>$\lambda=0$ 이면 pure predictive optimization</li>
  <li>$\lambda\to\infty$ 이면 Bayes 쪽</li>
</ul>

<p>으로 이해할 수 있습니다.</p>

<p>이 부분이 좋은 이유는,
PVI를 “Bayes를 완전히 버리는 방법”이 아니라</p>

<blockquote>
  <p><strong>predictive robustness 와 Bayesian regularization 사이를 조절하는 방법</strong></p>
</blockquote>

<p>으로 볼 수 있게 해준다는 점입니다.</p>

<hr />

<h2 id="가장-중요한-해석-pvi는-implicit-hierarchical-expansion-이다">가장 중요한 해석: PVI는 implicit hierarchical expansion 이다</h2>

<p>이 논문에서 가장 인상적인 부분은,
PVI posterior 의 분산이 줄어들지 않는 현상을
“VI approximation error” 가 아니라
“parameter heterogeneity” 로 해석한다는 점입니다.</p>

<h3 id="normal-example">Normal example</h3>

<p>논문은 아주 간단한 예를 듭니다.</p>

<p>모델은</p>

\[y\mid \theta \sim \mathcal N(\theta, 1)\]

<p>인데, 실제 데이터 생성 과정은</p>

\[y \sim \mathcal N(0, 2)\]

<p>라고 합시다.</p>

<p>이때 exact Bayes posterior 는 데이터가 많아질수록
$\theta=0$ 주변의 점질량으로 수렴합니다.
즉, regular Bayes 는</p>

<blockquote>
  <p>“하나의 고정된 $\theta$ 가 있다”</p>
</blockquote>

<p>는 complete pooling view 를 끝까지 유지합니다.</p>

<p>그런데 PVI는 다르게 움직입니다.
논문에 따르면 이 경우 PVI posterior 는</p>

\[q(\theta)\to \mathcal N(0,\sqrt{3})\]

<p>로 수렴합니다.</p>

<p>왜냐하면 이 분포를 mixing 하면 posterior predictive 가</p>

\[\tilde y \sim \mathcal N(0, 2)\]

<p>를 정확히 재현할 수 있기 때문입니다.</p>

<p>즉 PVI는</p>

<ul>
  <li>“모델이 틀렸으니 uncertainty 가 0 으로 가면 안 된다”</li>
  <li>“오히려 이 분산은 population-level heterogeneity 를 설명하는 분산일 수 있다”</li>
</ul>

<p>고 해석합니다.</p>

<p>이 관점에서 저자들은 PVI를</p>

<blockquote>
  <p><strong>explicit hierarchical Bayes 를 직접 쓰지 않고도,<br />
parameter population distribution 을 학습하는 implicit hierarchical expansion</strong></p>
</blockquote>

<p>으로 봅니다.</p>

<p>이 해석은 상당히 강합니다.</p>

<ul>
  <li>standard Bayes: 하나의 true parameter 에 수렴</li>
  <li>PVI: 필요하다면 parameter distribution 자체에 수렴</li>
</ul>

<p>즉 misspecification 하에서는 posterior variance 가
“사라져야 할 approximation artifact” 가 아니라
“남아 있어야 하는 signal” 이 됩니다.</p>

<hr />

<h2 id="heterogeneity-detection">Heterogeneity Detection</h2>

<p>이제 위 해석을 practical 하게 쓰면,
PVI posterior 의 분산은 <strong>model check</strong> 도구가 됩니다.</p>

<p>논문의 주장은 대략 이렇습니다.</p>

<ul>
  <li>잘 지정된 모델이면 PVI도 결국 좁은 posterior 로 수렴</li>
  <li>모델이 틀렸거나 어떤 parameter 가 population 에서 실제로 변하면,
PVI posterior 가 넓게 남을 수 있음</li>
</ul>

<p>그래서</p>

<blockquote>
  <p><strong>PVI variance 가 크면, 해당 parameter 를 varying effect 나 hierarchical effect 로 확장해야 할 가능성</strong></p>
</blockquote>

<p>을 시사합니다.</p>

<p>물론 논문도 이 부분의 한계를 같이 인정합니다.</p>

<ul>
  <li>finite sample 에서는 variance 가 큰 이유가
정말 misspecification 때문인지,
아니면 PVI의 느린 수렴률 때문인지 구분이 쉽지 않습니다.</li>
  <li>따라서 heterogeneity detection 은 좋은 heuristic 이지만,
자동화된 정답 판정기로 받아들이면 위험합니다.</li>
</ul>

<p>그래도 “posterior가 넓으면 모델을 더 유연하게 바꿔라” 라는 메시지는
실전 모델링에서 꽤 유용합니다.</p>

<hr />

<h2 id="구현-측면에서-무엇이-새롭나">구현 측면에서 무엇이 새롭나?</h2>

<p>PVI는 아이디어만 보면 간단하지만,
실제로는 gradient 계산이 쉽지 않습니다.</p>

<p>특히 log score 에서는</p>

\[\log \int p(y_i\mid\theta)\,q_\phi(\theta)\,d\theta\]

<p>를 직접 최적화해야 하므로,
Monte Carlo 근사와 로그가 섞이면서 bias 문제가 생깁니다.</p>

<p>논문은 여기서 두 가지 포인트를 제안합니다.</p>

<h3 id="1-log-score-에-대한-rejection-sampling-기반-unbiased-gradient">1. Log score 에 대한 rejection-sampling 기반 unbiased gradient</h3>

<p>저자들은 finite minibatch 에서 생기는 bias 를 줄이기 위해
rejection sampling 을 이용한 unbiased gradient estimator 를 제안합니다.</p>

<p>이 부분은 단순히 “PVI라는 철학”만 이야기하는 논문이 아니라,</p>

<blockquote>
  <p><strong>실제로 stochastic optimization 이 가능하도록 gradient estimator 까지 설계한 논문</strong></p>
</blockquote>

<p>이라는 점에서 중요합니다.</p>

<h3 id="2-crps-기반-likelihood-free-optimization">2. CRPS 기반 likelihood-free optimization</h3>

<p>CRPS는 density evaluation 이 아니라 <strong>simulation</strong> 만 있으면 쓸 수 있기 때문에,
likelihood-free model 에 바로 적용됩니다.</p>

<p>즉,</p>

<ul>
  <li>posterior sample $\theta \sim q_\phi(\theta)$ 를 뽑고</li>
  <li>simulator 에 넣어 $y^{\mathrm{sim}} \sim p(y\mid\theta)$ 를 생성한 뒤</li>
  <li>관측 데이터와 simulation 사이의 discrepancy 를 줄이도록 학습</li>
</ul>

<p>할 수 있습니다.</p>

<p>이건 ABC, neural SBI 류 문제와도 자연스럽게 연결됩니다.</p>

<hr />

<h2 id="experiments">Experiments</h2>

<p>논문은 세 가지 대표 실험을 보여줍니다.</p>

<h3 id="1-golf-putting-misspecification-detection">1. Golf putting: misspecification detection</h3>

<p>Gelman and Nolan (2002)의 골프 퍼팅 데이터에서
logistic regression 과 geometric model 을 비교합니다.</p>

<ul>
  <li>misspecified 된 logistic regression 에서는
Bayes posterior 가 너무 좁고 잘못된 predictive curve 를 고릅니다.</li>
  <li>PVI는 posterior 를 더 넓게 잡아 predictive fit 을 개선합니다.</li>
  <li>반대로 geometric model 처럼 잘 맞는 경우에는
Bayes 와 PVI가 거의 같은 결론으로 갑니다.</li>
</ul>

<p>즉,</p>

<blockquote>
  <p><strong>PVI posterior 가 넓게 남는 현상 자체가 misspecification signal</strong></p>
</blockquote>

<p>이라는 것을 보여주는 예시입니다.</p>

<h3 id="2-us-election-analysis-model-expansion-guide">2. U.S. election analysis: model expansion guide</h3>

<p>미국 대선 turnout 데이터를 이용한 다층 로지스틱 회귀 예제에서는,
어떤 interaction 이 더 필요할지를 PVI variance 로 진단합니다.</p>

<ul>
  <li>VI는 misspecified model 에서도 계속 concentrate 합니다.</li>
  <li>PVI는 특정 state 에서 variance 가 더 크게 남는 것을 보여줍니다.</li>
  <li>저자들은 이를 근거로
“이 state 들은 coefficient 가 population 에서 vary 해야 한다”
고 해석합니다.</li>
</ul>

<p>즉 PVI는 단순 예측 개선을 넘어서,</p>

<blockquote>
  <p><strong>어떤 방향으로 hierarchical model 을 확장할지 알려주는 진단 도구</strong></p>
</blockquote>

<p>로 쓰입니다.</p>

<h3 id="3-cryoem-likelihood-free-inference">3. CryoEM: likelihood-free inference</h3>

<p>이 논문의 응용 측면에서 가장 강한 부분입니다.</p>

<p>cryoEM에서는</p>

<ul>
  <li>simulator 는 있지만</li>
  <li>likelihood 는 intractable 하고</li>
  <li>게다가 분자 구조 자체가 population 내에서 heterogeneous 합니다.</li>
</ul>

<p>이 상황에서 exact Bayes posterior 는
샘플 수가 많아질수록 오히려 <strong>과신한 point estimate</strong> 로 붕괴할 수 있습니다.</p>

<p>반면 PVI-CRPS 는</p>

<ul>
  <li>likelihood 계산 없이</li>
  <li>simulator 기반으로</li>
  <li>분자 opening angle 의 <strong>population distribution</strong> 을 직접 학습합니다.</li>
</ul>

<p>즉 이 예제에서는 $q_\phi(\theta)$ 가 단순한 variational artifact 가 아니라,
실제로 물리적으로 의미 있는 population distribution 으로 해석됩니다.</p>

<hr />

<h2 id="generalized-vi-와의-차이">Generalized VI 와의 차이</h2>

<p>이 논문은 기존 generalized Bayes / generalized VI 와도 비교합니다.</p>

<p>표면적으로 보면 둘 다</p>

<ul>
  <li>loss 를 바꾸고</li>
  <li>divergence 를 바꾸고</li>
  <li>Bayes를 일반화한다</li>
</ul>

<p>는 점에서 비슷해 보입니다.</p>

<p>하지만 논문의 주장은 분명합니다.</p>

<blockquote>
  <p><strong>PVI는 generalized VI와 “정신적으로” 다른 프레임워크다.</strong></p>
</blockquote>

<p>왜냐하면 generalized VI는 여전히</p>

<ul>
  <li>$(y,\theta)$ space 의 loss</li>
  <li>$\theta$ space 의 divergence</li>
</ul>

<p>를 중심으로 posterior 를 정의하는 반면,
PVI는</p>

<ul>
  <li><strong>posterior predictive distribution</strong></li>
  <li><strong>outcome-space scoring rule</strong></li>
</ul>

<p>을 중심으로 objective 를 짜기 때문입니다.</p>

<p>특히 log score 를 쓰더라도</p>

<ul>
  <li>generalized VI: $\mathbb E_q[\log p(y\mid\theta)]$</li>
  <li>PVI: $\log \int p(y\mid\theta)q(\theta)d\theta$</li>
</ul>

<p>라서 둘은 일반적으로 같지 않습니다.</p>

<p>이 차이 때문에 PVI는</p>

<blockquote>
  <p><strong>continuous stacking</strong> 혹은<br />
<strong>continuous predictive model averaging</strong></p>
</blockquote>

<p>처럼 읽는 편이 더 자연스럽습니다.</p>

<hr />

<h2 id="limitations">Limitations</h2>

<p>논문이 인정하는 한계도 분명합니다.</p>

<ol>
  <li><strong>score 선택이 중요합니다.</strong>
    <ul>
      <li>어떤 predictive property 를 중요하게 보는지에 따라 결과가 달라집니다.</li>
      <li>log score, interval score, CRPS 는 서로 다른 목적을 가집니다.</li>
    </ul>
  </li>
  <li><strong>IID / exchangeable 가정에 기대고 있습니다.</strong>
    <ul>
      <li>논문 전개는 기본적으로 conditionally IID setting 위에 서 있습니다.</li>
      <li>시계열, dependent data, structured data 로 가면 확장이 더 필요합니다.</li>
    </ul>
  </li>
  <li><strong>finite-sample 해석은 아직 조심해야 합니다.</strong>
    <ul>
      <li>posterior variance 가 큰 이유를
misspecification 과 느린 수렴 중 무엇으로 볼지 모호할 수 있습니다.</li>
    </ul>
  </li>
  <li><strong>parameter truth 보다는 predictive truth 에 더 가깝습니다.</strong>
    <ul>
      <li>따라서 해석의 중심은 “이 posterior가 진짜 posterior 인가?” 가 아니라
“이 posterior가 predictive distribution 을 잘 만들도록 돕는가?” 에 있어야 합니다.</li>
    </ul>
  </li>
</ol>

<hr />

<h2 id="summary">Summary</h2>

<p>제가 읽고 느낀 이 논문의 핵심은 네 가지입니다.</p>

<ol>
  <li><strong>PVI는 Bayes posterior approximation 이 아니라 predictive optimization 이다.</strong>
    <ul>
      <li>이 점을 놓치면 논문의 핵심을 거의 놓치게 됩니다.</li>
    </ul>
  </li>
  <li><strong>misspecification 하에서는 posterior variance 가 줄어들지 않는 것이 오히려 장점일 수 있다.</strong>
    <ul>
      <li>이 분산은 latent heterogeneity 나 model expansion 필요성을 드러냅니다.</li>
    </ul>
  </li>
  <li><strong>PVI는 implicit hierarchical Bayes 로 읽을 수 있다.</strong>
    <ul>
      <li>“parameter 하나” 대신 “parameter population” 을 학습하는 관점입니다.</li>
    </ul>
  </li>
  <li><strong>CRPS 덕분에 likelihood-free inference 로 자연스럽게 확장된다.</strong>
    <ul>
      <li>이 부분은 SBI 문맥에서 특히 매력적입니다.</li>
    </ul>
  </li>
</ol>

<p>개인적으로는 이 논문을</p>

<blockquote>
  <p><strong>“VI를 predictive scoring rule 관점으로 다시 세운 논문”</strong></p>
</blockquote>

<p>이라기보다,</p>

<blockquote>
  <p><strong>“Bayesian inference의 목표를 posterior accuracy 에서 predictive adequacy 로 옮겨 놓은 논문”</strong></p>
</blockquote>

<p>으로 읽는 것이 더 정확하다고 느꼈습니다.</p>

<p>정리하면 PVI는</p>

<ul>
  <li>exact posterior 를 근사하는 또 하나의 VI 변형이라기보다</li>
  <li><strong>misspecified world 에서 어떤 uncertainty 를 남겨야 하는가</strong> 를 다시 묻는 프레임워크</li>
</ul>

<p>에 가깝다고 할 수 있습니다.</p>

<hr />

<h2 id="references">References</h2>

<ul>
  <li><a href="https://arxiv.org/abs/2410.14843">Lai, J., Linero, A., &amp; Yao, Y. (2024; revised 2026-03-30). <em>Predictive variational inference: Learn the predictively optimal posterior distribution</em>. arXiv:2410.14843.</a></li>
</ul>]]></content><author><name>Junhee Kim</name></author><category term="Statistics" /><category term="Bayesian" /><category term="Machine Learning" /><category term="Predictive Variational Inference" /><category term="Variational Inference" /><category term="Simulation-Based Inference" /><category term="Model Misspecification" /><summary type="html"><![CDATA[이 포스팅은 Lai, Linero, Yao 의 2024년 arXiv 논문 “Predictive variational inference: Learn the predictively optimal posterior distribution” 를 읽고 정리한 글입니다. 이 글에서 참고한 버전은 2026년 3월 30일 공개된 v3 입니다.]]></summary></entry><entry><title type="html">Approximate Bayesian Computation Posterior</title><link href="https://joon0390.github.io/abc-posterior/" rel="alternate" type="text/html" title="Approximate Bayesian Computation Posterior" /><published>2026-03-23T00:00:00+09:00</published><updated>2026-03-23T00:00:00+09:00</updated><id>https://joon0390.github.io/ABCPosterior</id><content type="html" xml:base="https://joon0390.github.io/abc-posterior/"><![CDATA[<blockquote>
  <p>이 포스팅은 likelihood를 직접 계산하기 어려운 상황에서 사용되는 <strong>Approximate Bayesian Computation (ABC)</strong> 와, 그 결과 얻어지는 <strong>ABC posterior</strong>의 의미를 정리합니다.</p>
</blockquote>

<hr />

<h2 id="introduction">Introduction</h2>

<p>베이지안 추론의 목표는 관측 데이터 $y_{obs}$가 주어졌을 때 파라미터 $\theta$의 사후분포</p>

\[p(\theta \mid y_{obs})
\propto
p(y_{obs}\mid\theta)\,p(\theta)\]

<p>를 계산하는 것입니다.</p>

<p>하지만 실제 문제에서는</p>

<ul>
  <li>likelihood를 닫힌 형태로 쓸 수 없거나</li>
  <li>likelihood 계산이 지나치게 비싸거나</li>
  <li>모델은 시뮬레이터 형태로만 주어지고 확률밀도는 직접 계산할 수 없는</li>
</ul>

<p>경우가 자주 등장합니다.</p>

<p>예를 들어 population genetics, epidemiology, ecology, agent-based model 같은 분야에서는</p>

<blockquote>
  <p>“주어진 $\theta$에서 데이터를 <strong>생성(simulate)</strong> 할 수는 있지만,<br />
$p(y_{obs}\mid\theta)$를 <strong>직접 평가(evaluate)</strong> 하기는 어렵다.”</p>
</blockquote>

<p>는 상황이 흔합니다.</p>

<p>이럴 때 사용하는 대표적인 likelihood-free inference 방법이 바로 <strong>Approximate Bayesian Computation (ABC)</strong> 입니다.</p>

<hr />

<h2 id="abc의-핵심-아이디어">ABC의 핵심 아이디어</h2>

<p>ABC의 아이디어는 단순합니다.</p>

<ol>
  <li>Prior에서 $\theta$를 뽑는다.</li>
  <li>그 $\theta$로부터 가짜 데이터 $y^\star$를 시뮬레이션한다.</li>
  <li>$y^\star$가 실제 데이터 $y_{obs}$와 충분히 비슷하면 그 $\theta$를 accept한다.</li>
</ol>

<p>즉, likelihood를 직접 계산하는 대신</p>

<blockquote>
  <p>“이 파라미터가 실제 데이터를 <strong>그럴듯하게 재현하는가?</strong>”</p>
</blockquote>

<p>를 기준으로 posterior를 구성합니다.</p>

<p>가장 단순한 형태의 rejection ABC는 아래와 같습니다.</p>

<pre><code class="language-python">for t in range(T):
    theta = sample_prior()
    y_star = simulate(theta)

    if distance(S(y_star), S(y_obs)) &lt;= epsilon:
        accept(theta)
</code></pre>

<p>여기서</p>

<ul>
  <li>$S(\cdot)$ : summary statistic</li>
  <li>$\rho(\cdot,\cdot)$ : distance function</li>
  <li>$\epsilon &gt; 0$ : tolerance</li>
</ul>

<p>입니다.</p>

<hr />

<h2 id="abc-posterior의-정의">ABC Posterior의 정의</h2>

<p>관측 데이터 전체를 그대로 비교하기보다, 일반적으로는 요약통계량</p>

\[s_{obs} = S(y_{obs})\]

<p>를 사용합니다.</p>

<p>이때 rejection ABC로 accept된 $\theta$들은 다음 분포를 따릅니다.</p>

\[\pi_\epsilon(\theta \mid s_{obs})
\propto
\pi(\theta)\,
\Pr_\theta\!\left(
\rho\big(S(Y), s_{obs}\big)\le \epsilon
\right)\]

<p>또는 적분 형태로</p>

\[\pi_\epsilon(\theta \mid s_{obs})
\propto
\pi(\theta)
\int
\mathbf{1}\!\left\{
\rho\big(S(y), s_{obs}\big)\le\epsilon
\right\}
p(y\mid\theta)\,dy\]

<p>로 쓸 수 있습니다.</p>

<p>이 분포가 바로 <strong>ABC posterior</strong> 입니다.</p>

<p>이 식의 의미는 명확합니다.</p>

<ul>
  <li>Prior $\pi(\theta)$에서 시작하고</li>
  <li>그 $\theta$가 만들어내는 데이터가 관측 요약통계량과 가까울수록</li>
  <li>더 큰 가중치를 받아 posterior에서 더 자주 남게 됩니다.</li>
</ul>

<p>즉 ABC posterior는</p>

<blockquote>
  <p><strong>관측 데이터와 비슷한 시뮬레이션을 만들어내는 파라미터에 확률질량을 집중시키는 분포</strong></p>
</blockquote>

<p>라고 이해할 수 있습니다.</p>

<hr />

<h2 id="abc-posterior는-무엇을-근사하는가">ABC Posterior는 무엇을 근사하는가?</h2>

<p>이 부분이 가장 중요합니다.</p>

<p>ABC posterior는 일반적으로 원래의 정확한 posterior</p>

\[p(\theta\mid y_{obs})\]

<p>를 바로 주는 것이 아닙니다. 실제로는 두 단계의 근사가 들어갑니다.</p>

<p>첫 번째는 <strong>summary statistic 근사</strong>입니다.</p>

<p>ABC는 보통 $y_{obs}$ 전체가 아니라 $S(y_{obs})$만 사용하므로, $\epsilon \to 0$라고 해도 보통은</p>

\[\pi_\epsilon(\theta \mid s_{obs})
\to
p(\theta \mid S(y_{obs}))\]

<p>에 가까워집니다.</p>

<p>따라서 $S(\cdot)$가 sufficient statistic이 아닐 경우,</p>

\[p(\theta\mid S(y_{obs})) \neq p(\theta\mid y_{obs})\]

<p>가 되어 정보 손실이 발생합니다.</p>

<p>두 번째는 <strong>tolerance 근사</strong>입니다.</p>

<p>$\epsilon &gt; 0$이면 exact match가 아니라 “충분히 가까움”만 요구하므로, 추가적인 smoothing이 들어갑니다. 즉</p>

\[\pi_\epsilon(\theta \mid s_{obs})
\approx
p(\theta \mid S(y_{obs}))\]

<p>이며, $\epsilon$이 클수록 근사는 거칠어집니다.</p>

<p>정리하면 ABC의 오차는 대체로 다음 두 가지에서 옵니다.</p>

<ol>
  <li><strong>summary statistic 선택으로 인한 정보 손실</strong></li>
  <li><strong>finite tolerance $\epsilon$로 인한 smoothing bias</strong></li>
</ol>

<p>따라서 ABC posterior를 해석할 때는</p>

<blockquote>
  <p>“이것은 true posterior의 근사인가?”<br />
보다는<br />
“어떤 summary와 어떤 tolerance 아래에서 정의된 posterior인가?”</p>
</blockquote>

<p>를 먼저 보는 것이 중요합니다.</p>

<hr />

<h2 id="kernel-abc-관점">Kernel ABC 관점</h2>

<p>위의 rejection ABC는 indicator kernel</p>

\[\mathbf{1}\{\rho(S(y), s_{obs})\le \epsilon\}\]

<p>을 사용한 형태로 볼 수 있습니다.</p>

<p>보다 일반적으로는 kernel weight를 사용해</p>

\[\pi_\epsilon^K(\theta \mid s_{obs})
\propto
\pi(\theta)
\int
K_\epsilon\!\left(
\rho(S(y), s_{obs})
\right)
p(y\mid\theta)\,dy\]

<p>로 정의할 수 있습니다.</p>

<p>여기서 $K_\epsilon(\cdot)$는 거리가 작을수록 큰 값을 주는 kernel입니다.</p>

<p>이 관점의 장점은 acceptance/rejection만 하는 대신, 관측치에 더 가까운 시뮬레이션에 더 큰 가중치를 줄 수 있다는 점입니다. 그래서 ABC posterior는 단순한 “필터링 결과”라기보다,</p>

<blockquote>
  <p><strong>관측 요약통계량 주변을 kernel로 부드럽게 감싼 posterior</strong></p>
</blockquote>

<p>로도 해석할 수 있습니다.</p>

<hr />

<h2 id="summary-statistic의-역할">Summary Statistic의 역할</h2>

<p>ABC에서 summary statistic은 사실상 모델링의 핵심입니다.</p>

<p>이상적으로는 low-dimensional이면서도 $\theta$에 대한 정보를 충분히 보존하는 $S(y)$가 필요합니다. 하지만 실제로는 두 요구가 충돌합니다.</p>

<ul>
  <li>summary dimension이 너무 크면 관측치와 시뮬레이션이 가까워질 확률이 급격히 줄어듭니다.</li>
  <li>너무 단순한 summary를 쓰면 중요한 정보가 사라져 posterior가 지나치게 넓어지거나 왜곡됩니다.</li>
</ul>

<p>즉 ABC는 흔히</p>

<blockquote>
  <p><strong>likelihood-free</strong> 이지만,<br />
<strong>summary-design-free</strong> 하지는 않습니다.</p>
</blockquote>

<p>고차원 데이터에서 raw data 자체를 비교하면 acceptance rate가 거의 0에 가까워지는 경우가 많기 때문에, 평균, 분산, quantile, autocorrelation, regression coefficient 같은 domain-specific summary가 많이 사용됩니다.</p>

<p>최근에는 neural network를 이용해 informative summary를 학습하는 방향도 많이 연구되고 있습니다.</p>

<hr />

<h2 id="tolerance-epsilon의-역할">Tolerance $\epsilon$의 역할</h2>

<p>$\epsilon$은 계산 효율과 정확도 사이의 trade-off를 조절합니다.</p>

<ul>
  <li>$\epsilon$이 작을수록 posterior는 더 정밀해지지만 accept되는 샘플 수가 줄어듭니다.</li>
  <li>$\epsilon$이 클수록 계산은 쉬워지지만 posterior bias가 커집니다.</li>
</ul>

<p>연속형 데이터에서는 exact equality가 사실상 확률 0이기 때문에, $\epsilon$은 거의 항상 필요합니다.</p>

<p>실무에서는 보통</p>

<ul>
  <li>파일럿 시뮬레이션으로 distance 분포를 본 뒤</li>
  <li>하위 몇 % quantile을 기준으로 $\epsilon$을 정하거나</li>
  <li>SMC-ABC에서 점차 $\epsilon$을 줄여가는</li>
</ul>

<p>방식을 사용합니다.</p>

<hr />

<h2 id="대표적인-abc-알고리즘">대표적인 ABC 알고리즘</h2>

<h3 id="rejection-abc">Rejection ABC</h3>

<p>가장 단순하고 직관적인 방법입니다. 구현이 쉽지만 acceptance rate가 매우 낮을 수 있습니다.</p>

<h3 id="mcmc-abc">MCMC-ABC</h3>

<p><a href="/mcmc/">MCMC</a>의 proposal 구조를 이용하되, likelihood 대신 ABC acceptance criterion을 사용합니다. prior 샘플링보다 효율적일 수 있지만 mixing이 느려질 수 있습니다.</p>

<h3 id="smc-abc">SMC-ABC</h3>

<p>큰 tolerance에서 시작하여 점차 작은 tolerance로 내려가면서 particle을 이동시킵니다. 현재는 실무에서 가장 널리 쓰이는 방식 중 하나입니다.</p>

<p>ABC posterior를 더 정교하게 근사하면서도 rejection ABC보다 훨씬 효율적입니다.</p>

<hr />

<h2 id="abc-posterior의-장점과-한계">ABC Posterior의 장점과 한계</h2>

<h3 id="장점">장점</h3>

<ol>
  <li>
    <p><strong>Likelihood-free inference</strong><br />
likelihood를 직접 계산할 수 없는 simulator-based model에도 적용 가능합니다.</p>
  </li>
  <li>
    <p><strong>모델 유연성</strong><br />
확률밀도를 수식으로 쓰지 못해도 시뮬레이터만 있으면 posterior inference를 시도할 수 있습니다.</p>
  </li>
  <li>
    <p><strong>직관적인 해석</strong><br />
“관측 데이터를 얼마나 잘 재현하는가”라는 기준으로 posterior를 구성하므로 이해가 비교적 쉽습니다.</p>
  </li>
</ol>

<h3 id="한계">한계</h3>

<ol>
  <li>
    <p><strong>Summary statistic 의존성</strong><br />
어떤 summary를 쓰느냐에 따라 posterior가 크게 달라질 수 있습니다.</p>
  </li>
  <li>
    <p><strong>Curse of dimensionality</strong><br />
데이터나 summary 차원이 커지면 accept/reject 기반 접근이 급격히 비효율적이 됩니다.</p>
  </li>
  <li>
    <p><strong>Approximation bias</strong><br />
$\epsilon$과 summary choice 때문에 true posterior와 차이가 날 수 있습니다.</p>
  </li>
  <li>
    <p><strong>시뮬레이션 비용</strong><br />
하나의 posterior sample을 얻기 위해 매우 많은 시뮬레이션이 필요할 수 있습니다.</p>
  </li>
</ol>

<hr />

<h2 id="summary">Summary</h2>

<ul>
  <li><strong>ABC posterior</strong>는 likelihood를 직접 계산하는 대신, 시뮬레이션 데이터가 관측 데이터와 얼마나 가까운지를 바탕으로 정의되는 사후분포입니다.</li>
  <li>rejection ABC에서 accept된 파라미터의 분포는</li>
</ul>

\[\pi_\epsilon(\theta \mid s_{obs})
\propto
\pi(\theta)\,
\Pr_\theta\!\left(
\rho\big(S(Y), s_{obs}\big)\le\epsilon
\right)\]

<p>로 표현됩니다.</p>

<ul>
  <li>$\epsilon \to 0$이면 보통 $p(\theta\mid S(y_{obs}))$에 가까워지며, $S(\cdot)$가 sufficient statistic일 때에만 true posterior에 수렴합니다.</li>
  <li>따라서 ABC의 성능은 <strong>simulator</strong>, <strong>summary statistic</strong>, <strong>distance</strong>, <strong>tolerance</strong>의 네 요소에 의해 결정됩니다.</li>
</ul>

<p>결국 ABC posterior는 단순히 “근사 posterior”라기보다,</p>

<blockquote>
  <p><strong>요약된 데이터와 허용 오차 아래에서 정의된 likelihood-free posterior</strong></p>
</blockquote>

<p>라고 보는 것이 가장 정확합니다.</p>

<p>읽어주셔서 감사합니다.</p>

<hr />

<h2 id="references">References</h2>

<ul>
  <li><a href="https://www.genetics.org/content/162/4/2025">Beaumont, M. A., Zhang, W., &amp; Balding, D. J. (2002). Approximate Bayesian Computation in Population Genetics.</a></li>
  <li><a href="https://doi.org/10.1214/11-STS378">Marin, J.-M., Pudlo, P., Robert, C. P., &amp; Ryder, R. J. (2012). Approximate Bayesian Computational Methods.</a></li>
  <li><a href="https://www.routledge.com/Handbook-of-Approximate-Bayesian-Computation/Sisson-Fan-Beaumont/p/book/9781138633215">Sisson, S. A., Fan, Y., &amp; Beaumont, M. (2018). Handbook of Approximate Bayesian Computation.</a></li>
  <li><a href="https://arxiv.org/abs/1707.01254">Lintusaari, J., Gutmann, M. U., Dutta, R., Kaski, S., &amp; Corander, J. (2017). Fundamentals and Recent Developments in Approximate Bayesian Computation.</a></li>
</ul>]]></content><author><name>Junhee Kim</name></author><category term="Statistics" /><category term="Bayesian" /><category term="Computation" /><category term="Approximate Bayesian Computation" /><category term="ABC" /><category term="Likelihood-free Inference" /><summary type="html"><![CDATA[이 포스팅은 likelihood를 직접 계산하기 어려운 상황에서 사용되는 Approximate Bayesian Computation (ABC) 와, 그 결과 얻어지는 ABC posterior의 의미를 정리합니다.]]></summary></entry><entry><title type="html">[Paper Review] Sampling Can Be Faster Than Optimization</title><link href="https://joon0390.github.io/sampling-can-be-faster-than-optimization/" rel="alternate" type="text/html" title="[Paper Review] Sampling Can Be Faster Than Optimization" /><published>2026-02-10T00:00:00+09:00</published><updated>2026-02-10T00:00:00+09:00</updated><id>https://joon0390.github.io/SamplingCanbeFasterthanOptimization</id><content type="html" xml:base="https://joon0390.github.io/sampling-can-be-faster-than-optimization/"><![CDATA[<blockquote>
  <p>이 포스팅은 <strong>Ma, Chen, Jin, Flammarion, Jordan (2019)</strong> 의 논문<br />
<a href="https://arxiv.org/pdf/1811.08413"><em>Sampling Can Be Faster Than Optimization</em></a> 을 읽고 정리한 글입니다.</p>
</blockquote>

<hr />

<h2 id="1-bigger-picture-two-tools-for-inference">1. Bigger Picture: Two Tools for Inference</h2>

<p>머신러닝에서 파라미터를 추정할 때 우리는 보통 두 가지 접근을 씁니다.</p>

<ul>
  <li><strong>최적화(optimization)</strong>: 손실을 최소화하는 한 점(point estimate)을 찾음</li>
  <li><strong>샘플링(sampling)</strong>: 분포 전체(불확실성 포함)를 근사함</li>
</ul>

<p>실무에서는 대개 “먼저 최적화로 빠르게 답을 찾고, 필요하면 샘플링으로 불확실성을 본다”는 흐름이 자연스럽습니다.<br />
그래서 암묵적으로 다음 통념이 자리 잡았습니다.</p>

<blockquote>
  <p>“최적화는 빠르고, 샘플링은 정확하지만 느리다.”</p>
</blockquote>

<p>문제는 이 통념이 주로 <strong>볼록(convex) / 로그볼록(log-concave)</strong> 세계에서 정립되었다는 점입니다.<br />
이 논문은 한 단계 더 일반적인 상황, 즉 <strong>국소적으로만 비볼록인 문제</strong>로 질문을 확장합니다.</p>

<blockquote>
  <p>“Local nonconvexity에서는 여전히 최적화가 샘플링보다 본질적으로 빠른가?”</p>
</blockquote>

<p>논문의 핵심 답은 의외입니다. 특정 문제 클래스에서는 <strong>샘플링은 차원 $d$</strong> 에 대해 다항 시간 복잡도를 가지지만, 최적화는 <strong>지수 시간 lower bound</strong>를 가질 수 있습니다.</p>

<hr />

<h2 id="2-setup-local-nonconvexity">2. Setup: Local Nonconvexity</h2>

<p>논문은 목적함수 $U:\mathbb{R}^d \to \mathbb{R}$ 에 대해 아래를 가정합니다.</p>

<ol>
  <li>$U$ 는 $L$-smooth (gradient Lipschitz)</li>
  <li>반지름 $R$ 바깥에서는 $m$-strongly convex</li>
  <li>$\nabla U(0)=0$ (기술적 가정)</li>
</ol>

<p>즉, 큰 영역에서는 잘 behaved하고, <strong>안쪽 bounded region에서만 비볼록성</strong>이 존재하는 구조입니다.</p>

<p>타깃 분포는</p>

\[p^*(x)\propto e^{-U(x)}\]

<p>이고, 조건수는 $\kappa=L/m$ 로 둡니다.</p>

<hr />

<h2 id="3-methods-compared-gd-vs-langevin">3. Methods Compared: GD vs Langevin</h2>

<p>가장 단순화해서 보면 업데이트는 다음과 같습니다.</p>

<ul>
  <li>Gradient Descent:
\(x_{k+1}=x_k-h_k\nabla U(x_k)\)</li>
  <li>ULA:
\(x_{k+1}=x_k-h_k\nabla U(x_k)+\xi_k,\quad \xi_k\sim\mathcal{N}(0,2h_k I)\)</li>
  <li>MALA: ULA proposal + Metropolis accept/reject</li>
</ul>

<p>핵심은 ULA가 GD와 거의 같은 형태인데, 잡음을 넣어 전역 구조를 탐색한다는 점입니다.</p>

<hr />

<h2 id="4-key-result-1-polynomial-time-sampling">4. Key Result 1: Polynomial-Time Sampling</h2>

<p>논문의 Theorem 1(ULA/MALA mixing time upper bound)을 요약하면,
\(\tau_{\text{ULA}}(\varepsilon)=\tilde{O}\!\left(e^{32LR^2}\kappa^2\frac{d}{\varepsilon^2}\right),\)
\(\tau_{\text{MALA}}(\varepsilon)=\tilde{O}\!\left(e^{40LR^2}\kappa^{3/2}d^{1/2}(d\log\kappa+\log(1/\varepsilon))^{3/2}\right).\)</p>

<p>$LR^2=O(\log d)$이면, 차원 $d$에 대해 <strong>다항 시간</strong>으로 제어됩니다.</p>

<p>논문의 이론 전개는</p>
<ul>
  <li>연속시간 확산과정의 KL 감소율 분석</li>
  <li>log-Sobolev constant 하한 도출</li>
  <li>이산화(ULA/MALA) 오차 제어
순서로 진행됩니다.</li>
</ul>

<hr />

<h2 id="5-key-result-2-exponential-lower-bound-for-optimization">5. Key Result 2: Exponential Lower Bound for Optimization</h2>

<p>Theorem 2는 더 강합니다.</p>

\[K=\Omega\!\left(\left(\frac{LR^2}{\varepsilon}\right)^{d/2}\right)\]

<p>즉, 함수값/고계도함수 질의를 허용하는 매우 넓은 알고리즘 클래스에서도,<br />
$\varepsilon$-최적해를 찾는 데 필요한 반복 수가 차원에 대해 지수적으로 커질 수 있습니다.</p>

<p>직관은 “볼 안에 지수 개의 작은 basin을 packing할 수 있다”는 구성입니다.<br />
최적화는 “어느 basin이 진짜 global optimum인지”를 맞혀야 하므로 조합 폭발이 생깁니다.</p>

<hr />

<h2 id="6-can-annealed-sampling-bypass-optimization">6. Can Annealed Sampling Bypass Optimization?</h2>

<p>논문은 $q_\beta^*(x)\propto e^{-\beta U(x)}$ 를 크게 sharpen해서<br />
최적점 근처를 샘플링하는 아이디어(시뮬레이티드 어닐링 계열)도 함께 점검합니다.</p>

<p>결과적으로, 높은 확률로 $\varepsilon$-정확도를 얻으려면<br />
$\beta=\tilde{\Omega}(d/\varepsilon)$가 필요해져서 전체 계산량은 다시 지수 스케일이 됩니다.</p>

<p>즉, 이 설정에서는 “샘플링을 최적화 대용으로 쓰는 것”까지는 쉽지 않다는 메시지입니다.</p>

<hr />

<h2 id="7-gaussian-mixture-model-case-study">7. Gaussian Mixture Model Case Study</h2>

<p>논문은 GMM mean 추정에서 ULA/MALA와 EM을 비교합니다.</p>

<ul>
  <li>조건 $MR^2=O(\log d)$ 하에서 ULA/MALA는 이론적으로 다항 복잡도</li>
  <li>반면 EM은 초기화가 나쁘면 고차원에서 급격히 어려워짐</li>
</ul>

<p>실험(Figure 2)에서도 차원이 커질수록</p>
<ul>
  <li>ULA는 비교적 완만하게 증가</li>
  <li>EM은 필요한 gradient query가 급증
하는 경향을 보여줍니다.</li>
</ul>

<hr />

<h2 id="8-key-takeaways">8. Key Takeaways</h2>

<ol>
  <li>
    <p>이 논문은 “샘플링 vs 최적화”를 정확히 비교하기 위해<br />
<strong>동일한 local nonconvex class</strong> 위에서 복잡도 경계를 맞춰 제시합니다.</p>
  </li>
  <li>
    <p>샘플링 복잡도는 확률질량의 “전역 구조”에 더 민감하고,<br />
최적화 복잡도는 “국소 basin 탐색”의 조합 복잡도에 민감하다는 대비가 명확합니다.</p>
  </li>
  <li>
    <p>“샘플링이 항상 느리다”는 통념은 convex 세계에서는 맞지만,<br />
nonconvex 세계에서는 반례가 충분히 생길 수 있음을 보여줍니다.</p>
  </li>
</ol>

<hr />

<h2 id="9-limitations-and-practical-interpretation">9. Limitations and Practical Interpretation</h2>

<ul>
  <li>결과는 <strong>worst-case 복잡도 경계</strong>입니다. 모든 실제 문제에서 샘플링이 빠르다는 뜻은 아닙니다.</li>
  <li>상수항 $e^{O(LR^2)}$ 가 크면 이론적 다항성이 실무 시간으로 바로 이어지지 않을 수 있습니다.</li>
  <li>MCMC는 mixing/diagnostics가 필수라 구현 난이도는 여전히 있습니다.</li>
</ul>

<hr />

<h2 id="10-conclusion">10. Conclusion</h2>

<p>이 논문의 가장 중요한 메시지는 아래 한 줄입니다.</p>

<blockquote>
  <p>비볼록 문제에서는, “최적화가 샘플링보다 무조건 빠르다”는 일반 명제가 성립하지 않는다.</p>
</blockquote>

<p>특히 mixture-like landscape처럼 basin이 많은 구조에서는<br />
샘플링이 전역 구조를 따라 다항 시간으로 움직일 수 있고,<br />
최적화는 basin 식별 자체가 지수적으로 어려울 수 있다는 점이 인상적입니다.</p>

<hr />

<h2 id="reference">Reference</h2>

<ul>
  <li>Ma, Y.-A., Chen, Y., Jin, C., Flammarion, N., &amp; Jordan, M. I. (2019).<br />
<em>Sampling Can Be Faster Than Optimization</em>. arXiv:1811.08413.<br />
https://arxiv.org/abs/1811.08413</li>
</ul>]]></content><author><name>Junhee Kim</name></author><category term="Machine Learning" /><category term="Bayesian" /><category term="Optimization" /><category term="MCMC" /><category term="Langevin Dynamics" /><category term="ULA" /><category term="MALA" /><category term="Nonconvex Optimization" /><summary type="html"><![CDATA[이 포스팅은 Ma, Chen, Jin, Flammarion, Jordan (2019) 의 논문 Sampling Can Be Faster Than Optimization 을 읽고 정리한 글입니다.]]></summary></entry><entry><title type="html">Gibbs Posteriors</title><link href="https://joon0390.github.io/gibbs-posteriors/" rel="alternate" type="text/html" title="Gibbs Posteriors" /><published>2025-12-30T00:00:00+09:00</published><updated>2025-12-30T00:00:00+09:00</updated><id>https://joon0390.github.io/GibbsPosterior</id><content type="html" xml:base="https://joon0390.github.io/gibbs-posteriors/"><![CDATA[<blockquote>
  <p>이 포스팅은 모델 Misspecification에 강건한 <strong>Gibbs Posteriors</strong> (or <strong>Generalized Posteriors</strong>)에 대해 정리합니다.</p>
</blockquote>

<h2 id="introduction-standard-bayes의-한계">Introduction: Standard Bayes의 한계</h2>

<p>이전 포스팅에서 다룬 <a href="/variational-inference/"><strong>Variational Inference</strong></a>를 포함한 대부분의 베이지안 추론은 “우리가 가정한 Likelihood 모델 $p(y \mid \theta)$가 실제 데이터 생성 과정을 포함하고 있다”고 가정합니다.</p>

<p>하지만 실제 데이터는 다음과 같은 상황에 처할 때가 많습니다.</p>

<ol>
  <li><strong>Model Misspecification</strong>: 실제 분포가 가우시안이 아닌데 가우시안 Likelihood를 사용하는 경우.</li>
  <li><strong>Outliers</strong>: 소수의 이상치가 Likelihood에 과도한 영향을 주어 Posterior를 왜곡하는 경우.</li>
  <li><strong>Complex Loss</strong>: 단순히 데이터의 생성 확률을 높이는 것보다, 특정 Loss 함수(예: Hinge loss, MAE)를 최소화하는 파라미터 $\theta$를 찾고 싶은 경우.</li>
</ol>

<p>이런 상황에서 Likelihood 대신 <strong>임의의 Loss 함수를 직접 사용하여 Posterior를 정의</strong>하는 방법이 바로 <strong>Gibbs Posteriors</strong>입니다.</p>

<h2 id="gibbs-posterior의-정의">Gibbs Posterior의 정의</h2>

<p>Standard Bayesian의 Posterior는 다음과 같습니다.
\(p(\theta \mid y) \propto \exp\left( \log p(y \mid \theta) \right) p(\theta)\)</p>

<p>여기서 $\log p(y \mid \theta)$를 일반적인 Loss 함수 $L_n(y, \theta)$로 치환하고, 학습의 속도를 조절하는 <strong>Learning rate $\eta$</strong> 를 도입하면 <strong>Gibbs Posterior</strong>가 정의됩니다.</p>

\[\pi_n(\theta) \propto \exp\left( -\eta \cdot L_n(y, \theta) \right) \pi_0(\theta)\]

<ul>
  <li>$L_n(y, \theta)$: 데이터를 평가할 Loss 함수 (ex: $\sum (y_i - f_\theta(x_i))^2$)</li>
  <li>$\pi_0(\theta)$: Prior</li>
  <li>$\eta &gt; 0$: <strong>Learning rate</strong> (또는 Inverse Temperature). 데이터로부터 얼마나 적극적으로 정보를 수용할지 결정합니다.</li>
</ul>

<h2 id="왜-gibbs인가">왜 “Gibbs”인가?</h2>

<p>이 명칭은 통계 역학의 <a href="https://en.wikipedia.org/wiki/Boltzmann_distribution"><strong>Gibbs Distribution</strong> or Boltzmann Distribution</a>에서 유래했습니다. 에너지(Loss)가 낮은 상태에 더 높은 확률을 부여하는 구조가 동일하기 때문입니다.</p>

<ul>
  <li><strong>Energy</strong> $\leftrightarrow$ <strong>Loss Function</strong></li>
  <li><strong>Temperature</strong> $\leftrightarrow$ <strong>$1/\eta$</strong></li>
</ul>

<p>$\eta$가 커질수록(온도가 낮아질수록) Posterior는 Loss를 최소화하는 지점 주변으로 아주 좁게 집중됩니다.</p>

<h2 id="variational-gibbs-inference-vgi">Variational Gibbs Inference (VGI)</h2>

<p>Gibbs Posterior 역시 정규화 상수를 구하기 어렵기 때문에, VI를 통해 근사할 수 있습니다. Gibbs Posterior를 타겟으로 하는 VI의 목적 함수(Generalized ELBO)는 다음과 같습니다.</p>

\[\mathcal{L}_{Gibbs}(q) = \mathbb{E}_q[-\eta L_n(y, \theta)] - \text{KL}(q(\theta) \,\|\, \pi_0(\theta))\]

<p>이를 최대화하는 것은 결국 다음의 두 항을 최적화하는 것과 같습니다.</p>
<ol>
  <li><strong>$\mathbb{E}_q[L_n(y, \theta)]$ 최소화</strong>: 기대 손실을 줄여 데이터에 적합시킴.</li>
  <li><strong>$\text{KL}(q | \pi_0)$ 최소화</strong>: Prior에서 너무 멀어지지 않도록 규제(Regularization).</li>
</ol>

<p>이 수식은 <strong>PAC-Bayesian Bound</strong>와도 직접적으로 연결됩니다. 특정 조건하에서 이 목적 함수를 최적화하여 얻은 $q$는 본 적 없는 데이터에 대한 Generalization Error의 상한을 최소화하는 분포임이 증명되어 있습니다.</p>

<h2 id="핵심-파라미터-eta-의-선택">핵심 파라미터: $\eta$ 의 선택</h2>

<p>Gibbs Posterior에서 가장 까다로운 부분은 $\eta$의 값을 결정하는 것입니다.</p>

<ul>
  <li>$\eta$가 너무 크면: 모델이 데이터의 노이즈나 이상치에 과적합(Overfitting)됩니다.</li>
  <li>$\eta$가 너무 작으면: 데이터로부터 충분히 배우지 못하고 Prior에 머물게 됩니다(Underfitting).</li>
</ul>

<p>최근 연구들(SafeBayes 등)은 데이터의 증거(evidence)를 기반으로 최적의 $\eta$를 자동으로 선택하는 알고리즘을 제안하고 있습니다.</p>

<h2 id="gibbs-posteriors의-장점">Gibbs Posteriors의 장점</h2>

<ol>
  <li><strong>Robustness</strong>: 이상치에 민감한 Squared error 대신 Huber loss나 Absolute error를 사용하여 더 강건한 추론이 가능합니다.</li>
  <li><strong>Flexibility</strong>: 모델의 확률적 구조(Likelihood)를 엄밀하게 설계하지 않아도, 목적에 맞는 Loss만 있다면 베이지안 추론의 틀을 사용할 수 있습니다.</li>
  <li><strong>Bayesian Deep Learning과의 연관성</strong>: 신경망에서 사용하는 Cross-entropy 등을 Loss로 직접 사용하여 Bayesian Neural Networks를 구성할 때 이론적 근거를 제공합니다.</li>
</ol>

<h2 id="summary">Summary</h2>

<ul>
  <li><strong>Gibbs Posterior</strong>는 Likelihood 대신 Loss 함수를 지수화하여 Posterior를 구성하는 방식이다.</li>
  <li>모델 오설정이나 이상치가 존재하는 실제 환경에서 Standard Bayes보다 강건한 성능을 보인다.</li>
</ul>

<h2 id="reference">Reference</h2>

<ul>
  <li><a href="https://academic.oup.com/jrsssb/article/78/5/1103/7036237">Bissiri, P. G., et al. (2016). A General Framework for Updating Belief Distributions.</a></li>
  <li><a href="https://www.tandfonline.com/doi/abs/10.1198/016214506000001437">Gneiting, T., &amp; Raftery, A. E. (2007). Strictly Proper Scoring Rules, Prediction, and Estimation.</a></li>
  <li><a href="https://arxiv.org/abs/2110.11216">Alquier, P. (2021). User’s Guide to PAC-Bayes.</a></li>
</ul>]]></content><author><name>Junhee Kim</name></author><category term="Statistics" /><category term="Bayesian" /><category term="Gibbs Posteriors" /><category term="Generalized Bayes" /><category term="PAC-Bayes" /><summary type="html"><![CDATA[이 포스팅은 모델 Misspecification에 강건한 Gibbs Posteriors (or Generalized Posteriors)에 대해 정리합니다.]]></summary></entry><entry><title type="html">[Paper Review] Generalized Variational Inference: Three arguments for deriving new posteriors</title><link href="https://joon0390.github.io/generalized-variational-inference/" rel="alternate" type="text/html" title="[Paper Review] Generalized Variational Inference: Three arguments for deriving new posteriors" /><published>2025-12-06T00:00:00+09:00</published><updated>2025-12-06T00:00:00+09:00</updated><id>https://joon0390.github.io/GVI</id><content type="html" xml:base="https://joon0390.github.io/generalized-variational-inference/"><![CDATA[<blockquote>
  <p>이 포스팅은 Knoblauch, Jewson, Damoulas (2019) 의<br />
<a href="https://arxiv.org/pdf/1904.02063">“Generalized Variational Inference: Three arguments for deriving new posteriors”</a> 를 읽고 정리한 글입니다.</p>
</blockquote>

<hr />

<h2 id="introduction">Introduction</h2>

<h3 id="problem-and-proposed-solution">Problem and Proposed Solution</h3>

<p>베이지안 추론은 다음과 같은 형태의 posterior 를 사용합니다.</p>

\[q_B^*(\theta \mid x_{1:n})
\propto \pi(\theta)\; p_n(x_{1:n} \mid \theta)\]

<p>여기서 $\pi$ 는 prior, $p_n$ 은 likelihood 입니다.</p>

<p>논문 저자들은 이 posterior 를 실제로 사용하기 위해서는 암묵적으로 <strong>세 가지 가정</strong>이 수반된다고 강조합니다.</p>

<ol>
  <li><strong>Well-specified prior</strong>
    <ul>
      <li>사전 지식을 잘 반영하는 prior $\pi(\theta)$ 를 알고 있고, 그걸 수학적으로 잘 표기할 수 있다고 가정합니다.</li>
    </ul>
  </li>
  <li><strong>Well-specified likelihood model</strong>
    <ul>
      <li>실제 데이터의 분포가 $p_n(x_{1:n}\mid \theta)$ 꼴이라고 믿습니다. (모델이 참이라고 가정)</li>
    </ul>
  </li>
  <li><strong>Infinite computing power</strong>
    <ul>
      <li>일반적으로 intractable 한 posterior $q_B^*(\theta)$ 를 <strong>정확하게</strong> 다룰 수 있다고 봅니다.</li>
    </ul>
  </li>
</ol>

<p>&lt;/br&gt;</p>

<p>작거나 전통적인 통계 task 에서는 이 세 가지 가정이 그럭저럭 성립할 수 있지만,<br />
데이터 및 모델의 규모가 커질수록 현실과의 간극이 커집니다.</p>

<ul>
  <li>prior 는 사실상 “fake” 인 경우가 많습니다.</li>
  <li>likelihood 는 misspecification, heavy tails, contamination 이 흔합니다.</li>
  <li>posterior 는 너무 커서 MCMC, standard VI 로도 다루기 힘든 경우가 많습니다.</li>
</ul>

<p>&lt;/br&gt;</p>

<p>따라서 저자들은 아예 <strong>Bayes rule 을 최적화 문제로 재해석</strong>하고,<br />
그 틀을 일반화하여 “새로운 posterior” 를 설계하자고 제안합니다.</p>

<p>그 결과물이 바로</p>

<ul>
  <li><strong>Rule of Three (RoT)</strong> : 3개의 argument 로 정의되는 generalized posterior</li>
  <li><strong>GVI (Generalized Variational Inference)</strong> : RoT 의 tractable 한 특수 케이스</li>
</ul>

<p>입니다.</p>

<hr />

<h2 id="method-1">Method 1</h2>

<h3 id="bayes의-multiplicative-form">Bayes의 Multiplicative Form</h3>

<p>먼저 generalized Bayesian posterior 를 다음과 같이 씁니다.</p>

\[q_B^*(\theta)
\propto
\pi(\theta)\prod_{i=1}^n \exp\{-\ell(\theta, x_i)\}.\]

<ul>
  <li><strong>Standard Bayes</strong> 인 경우<br />
$\ell(\theta, x_i) = -\log p(x_i \mid \theta)$ 입니다.</li>
  <li><strong>Generalized Bayes</strong> 인 경우<br />
$\ell$ 은 임의의 loss function 입니다. (예: median 추론이면 $|\theta - x_i|$)</li>
</ul>

<p>이 multiplicative form 은 <strong>loss 를 quasi-likelihood</strong> 로 보는 관점에서도 해석할 수 있습니다.<br />
(예: Bissiri et al. 의 generalized Bayes)</p>

<hr />

<h3 id="bayes-posterior는-무한-차원에서의-최적화-문제">Bayes Posterior는 무한 차원에서의 최적화 문제</h3>

<p><a href="https://projecteuclid.org/journals/annals-of-probability/volume-3/issue-1/I-Divergence-Geometry-of-Probability-Distributions-and-Minimization-Problems/10.1214/aop/1176996454.full">Csiszár (1975)</a> 와<br />
<a href="https://onlinelibrary.wiley.com/doi/abs/10.1002/cpa.3160280102">Donsker &amp; Varadhan (1975)</a> 에 따르면,<br />
Bayes posterior 는 다음과 같은 <strong>무한 차원 최적화 문제의 해</strong>로 쓸 수 있습니다.</p>

\[q_B^*
=
\arg\min_{q \in \mathcal P(\Theta)}
\bigg\{
\mathbb{E}_q\bigg[\sum_{i=1}^n \ell(\theta, x_i)\bigg]
+ \mathrm{KL}(q \,\|\, \pi)
\bigg\}\]

<ul>
  <li>$\mathcal P(\Theta)$ : $\Theta$ 위의 <strong>모든 확률분포들의 집합</strong></li>
  <li>첫 항 : 데이터에 대한 <strong>loss 의 기댓값</strong></li>
  <li>둘째 항 : prior 와의 <strong>KLD regularization</strong></li>
</ul>

<p>즉,</p>

<blockquote>
  <p><strong>Bayes posterior 는<br />
“loss + KLD” 를 최소화하는 무한 차원 최적화 문제의 해입니다.</strong></p>
</blockquote>

<p>이것이 논문에서 계속 끌고 가는 핵심 isomorphism 입니다.</p>

<hr />

<h3 id="optimality-of-standard-vi">Optimality of Standard VI</h3>

<p>Exact posterior 는 계산 cost 가 크기 때문에,<br />
posterior 후보를 어떤 variational family<br />
$\mathcal Q = { q(\theta \mid \kappa) : \kappa \in K }$ 로 제한합니다.</p>

\[\text{original: } \min_{q \in \mathcal P(\Theta)} \;\;\longrightarrow\;\;
\text{constrained: } \min_{q \in \mathcal Q}\]

<p>그러면 최적화 문제는 다음과 같이 바뀝니다.</p>

\[q_{\mathrm{VI}}^*
=
\arg\min_{q\in \mathcal Q}
\bigg\{
\mathbb{E}_q\bigg[\sum_{i=1}^n \ell(\theta, x_i)\bigg]
+ \mathrm{KL}(q \,\|\, \pi)
\bigg\}\]

<p>이 식은 우리가 알고 있는 <strong>standard VI 의 objective (ELBO 최대화와 동치)</strong> 와 동일합니다.</p>

<blockquote>
  <p><strong>즉, standard VI 는<br />
variational family $\mathcal Q$ 가 고정된 상황에서<br />
Bayes optimization 문제의 “제한된 최적해”입니다.</strong></p>
</blockquote>

<p>같은 $\mathcal Q$ 를 사용하면서 <strong>다른 divergence</strong> 를 최적화하는 VI 들(α-VI, Rényi-VI 등)은<br />
이 base optimization 문제를 그대로 계승하지 않기 때문에,<br />
저자들은 이를 “Bayesian optimality 관점에서 suboptimal 하다”라고 봅니다.</p>

<hr />

<h2 id="method-2-rule-of-three-rot">Method 2: Rule of Three (RoT)</h2>

<h3 id="rot-세-개의-argument-로-정의되는-generalized-posterior">RoT: 세 개의 argument 로 정의되는 generalized posterior</h3>

<p>앞의 isomorphism 을 보고 나면 자연스럽게 다음과 같은 생각을 할 수 있습니다.</p>

<blockquote>
  <p>“그렇다면 <strong>loss, divergence, feasible set</strong> 을 바꾸면<br />
새로운 posterior 들을 통일된 방식으로 정의할 수 있지 않을까?”</p>
</blockquote>

<p>이 생각을 공식화한 것이 <strong>Rule of Three (RoT)</strong> 입니다.</p>

<p>수식으로 쓰면 다음과 같습니다.</p>

\[q^*(\theta)
=
\arg\min_{q\in \Pi}
\bigg\{
\mathbb{E}_q\bigg[\sum_{i=1}^n \ell(\theta, x_i)\bigg]
+ D(q \,\|\, \pi)
\bigg\}
\;\;\overset{\text{def}}{=}\;\;
P(\ell, D, \Pi)\]

<p>여기서 세 가지 argument 는 다음과 같습니다.</p>

<ol>
  <li><strong>Loss $\ell(\theta, x)$</strong>
    <ul>
      <li>반드시 $-\log p(x\mid\theta)$ 일 필요는 없습니다.</li>
      <li>
        <table>
          <tbody>
            <tr>
              <td>예: median 추론이면 absolute error $</td>
              <td>\theta - x</td>
              <td>$, robust loss, scoring rule 등입니다.</td>
            </tr>
          </tbody>
        </table>
      </li>
    </ul>
  </li>
  <li><strong>Divergence $D(q \,|\, \pi)$</strong>
    <ul>
      <li>prior 와 posterior 사이의 distance 입니다.</li>
      <li>Standard Bayes 에서는 $D = \mathrm{KL}$ 입니다.</li>
      <li>대신 Rényi divergence, β-divergence 등 다른 divergence 도 사용할 수 있습니다.</li>
    </ul>
  </li>
  <li><strong>Feasible set $\Pi \subset \mathcal P(\Theta)$</strong>
    <ul>
      <li>posterior 후보 분포들의 집합입니다.</li>
      <li>$\Pi = \mathcal P(\Theta)$ 이면 “full” generalized Bayes 입니다.</li>
      <li>$\Pi = \mathcal Q$ 이면 variational inference 가 됩니다.</li>
    </ul>
  </li>
</ol>

<p>이 RoT 안에서 여러 방법들을 special case 로 볼 수 있습니다.</p>

<ul>
  <li><strong>Standard Bayes</strong>
    <ul>
      <li>$\ell = -\log \text{Likelihood}$</li>
      <li>$D = \mathrm{KL}$</li>
      <li>$\Pi = \mathcal P(\Theta)$</li>
    </ul>
  </li>
  <li><strong>Standard VI</strong>
    <ul>
      <li>$\ell = -\log \text{Likelihood}$</li>
      <li>$D = \mathrm{KL}$</li>
      <li>$\Pi = \mathcal Q$ (variational family)</li>
    </ul>
  </li>
  <li><strong>Generalized Bayes</strong>
    <ul>
      <li>$\ell$ 또는 $D$ 를 수정하고, $\Pi$ 는 전체 공간으로 유지합니다.</li>
    </ul>
  </li>
</ul>

<hr />

<h3 id="axioms">Axioms</h3>

<p>RoT 가 만족해야 할 <strong>세 가지 공리(Axioms)</strong> 가 있습니다 (논문 4.1절).</p>

<p>대략적으로 정리하면 다음과 같습니다.</p>

<ol>
  <li><strong>Coherence / Consistency</strong>
    <ul>
      <li>loss 와 divergence 를 어떻게 조합해야<br />
“일관된 belief 업데이트” 가 되는지에 대한 조건입니다.</li>
    </ul>
  </li>
  <li><strong>Modular decomposition</strong>
    <ul>
      <li>loss 는 <strong>goodness of fit</strong> (어떤 $\theta$ 가 좋은지),</li>
      <li>divergence 는 <strong>belief regularization</strong> (prior 대비 얼마나 멀어질 수 있는지)<br />
를 담당해야 합니다.</li>
      <li>divergence 를 바꿀 때, loss 의 의미를 훼손하면 안 됩니다.</li>
    </ul>
  </li>
  <li><strong>Invariance / Reparameterization 등 기술적 조건</strong>
    <ul>
      <li>reparametrization 에 대해 잘 작동하는지,</li>
      <li>적절한 regularity / measurability 등을 만족하는지에 대한 조건입니다.</li>
    </ul>
  </li>
</ol>

<p>저자들은 <strong>이 공리들을 만족하는 generalized posterior 는 RoT 형태로 표현될 수 있다</strong>고 주장합니다.<br />
즉, RoT 는 “합리적인 generalized Bayesian 업데이트” 들을 하나의 형태로 묶는 프레임워크라고 볼 수 있습니다.</p>

<figure style="text-align: center;">
  <img src="/assets/img/gvi/image.png" alt="RoT" width="1000" />
</figure>

<hr />

<h2 id="method-3-generalized-variational-inference-gvi">Method 3: Generalized Variational Inference (GVI)</h2>

<p>이제 RoT 는 정의되었지만, 여전히 $\Pi = \mathcal P(\Theta)$ 로 두면 계산 cost 가 매우 크게 됩니다.</p>

<p>그래서 tractable 한 <strong>special case</strong> 로서 <strong>GVI</strong> 를 정의합니다.</p>

\[\Pi = \mathcal Q = \{q(\theta \mid \kappa) : \kappa \in K\}
\subset \mathcal P(\Theta)\]

<p>를 variational family 로 잡아서 RoT 를 푸는 것을 <strong>GVI</strong> 라고 부릅니다.</p>

<p>즉, GVI 의 objective 는 다음과 같습니다.</p>

\[q_{\mathrm{GVI}}^*
=
\argmin_{q \in \mathcal Q}
\bigg\{
\mathbb{E}_q\bigg[\sum_{i=1}^n \ell(\theta, x_i)\bigg]
+ D(q \,\|\, \pi)
\bigg\}\]

<ul>
  <li>$\ell$ : 원하는 loss (NLL, robust loss, scoring rule, pseudo-likelihood 등)</li>
  <li>$D$ : 원하는 divergence (KL, Rényi, β-divergence 등)</li>
  <li>$\mathcal Q$ : mean-field Gaussian, normalizing flow, BNN 등 variational family</li>
</ul>

<p>GVI 는 RoT 의 특수 케이스이기 때문에 <strong>Axiom 1, 2, 3 을 모두 만족</strong>합니다.</p>

<p>이로부터 다음과 같은 해석이 가능합니다.</p>

<ul>
  <li>Loss $\ell$ 은 <strong>어떤 $\theta$ 가 좋은가</strong>를 결정하는 역할,</li>
  <li>Divergence $D$ 는 <strong>prior 대비 posterior 가 얼마나 멀어질 수 있는가</strong>를 규제하는 역할을 합니다.</li>
</ul>

<p>따라서 두 요소를 <strong>분리해서 조절할 수 있습니다.</strong></p>

<ul>
  <li>$\ell$ 을 바꾸면 “best parameter” 의 선택 기준이 바뀌고,</li>
  <li>$D$ 를 바꾸면 <strong>uncertainty quantification</strong> 의 크기/모양만 바뀌는 식입니다.</li>
</ul>

<hr />

<h3 id="gvi-vs-dvi-discrepancy-vi">GVI vs DVI (Discrepancy VI)</h3>

<p>최근 VI 문헌에서는, posterior 와의 KL 대신 다른 divergence (또는 discrepancy) 를 쓰는 방식들이 많이 제안됩니다.</p>

<ul>
  <li>Rényi-α divergence 기반 VI</li>
  <li>χ²-divergence 기반 VI</li>
  <li>Wasserstein distance 기반 VI</li>
  <li>기타 f-divergence 기반 VI 등</li>
</ul>

<p>논문에서는 이런 계열을 통칭해 <strong>Discrepancy Variational Inference (DVI)</strong> 라고 부르며,<br />
RoT/GVI 관점에서 다음과 같이 비판합니다.</p>

<ol>
  <li><strong>같은 $\mathcal Q$ 를 쓰면서 posterior 와의 divergence 를 바꾸는 것은 Bayesian 의미에서 suboptimal</strong>입니다.
    <ul>
      <li>Bayes-as-optimization 관점에서 base 문제는 이미<br />
\(\min_q \big\{\mathbb E_q[\ell] + \mathrm{KL}(q\|\pi)\big\}\)
로 정해져 있습니다.</li>
      <li>variational family $\mathcal Q$ 로 제한하면,<br />
이 문제의 최적해는 <strong>KL 기반 VI 하나뿐</strong>입니다.</li>
      <li>posterior 와의 다른 divergence 를 직접 줄이는 DVI 는<br />
이 base optimization 의 “제한된 해” 가 아니므로,<br />
이러한 의미에서 <strong>Bayesian optimality 를 잃는다</strong>고 주장합니다.</li>
    </ul>
  </li>
  <li><strong>Modularity 가 깨집니다.</strong>
    <ul>
      <li>GVI (RoT) 에서는
        <ul>
          <li>$\ell$ 이 “어떤 $\theta$ 가 좋은가” 를</li>
          <li>$D$ 가 “prior 대비 얼마나 멀어질 수 있는가 / dispersion” 을<br />
담당하도록 설계되어 있습니다.</li>
        </ul>
      </li>
      <li>따라서 divergence 를 바꾸더라도, 이상적으로 <strong>argmin 위치(최선의 $\theta$)</strong> 는<br />
loss 가 결정하고 유지되며, $D$ 는 “그 주변에 mass 를 얼마나/어떻게 둘지” 만 조정해야 합니다.</li>
      <li>반면 DVI 는 posterior 와의 divergence 를 직접 최적화하기 때문에,<br />
divergence 를 바꾸는 순간 <strong>최적점 위치 자체까지 같이 움직여 버릴 수 있습니다.</strong><br />
→ loss 역할과 regularization 역할이 뒤엉기게 됩니다.</li>
    </ul>
  </li>
  <li><strong>실제 예시: Bimodal mixture 에서의 비직관적 posterior</strong>
    <ul>
      <li>논문은 simple bimodal Gaussian mixture 예시에서
        <ul>
          <li>standard VI (KL, mean-field) 와</li>
          <li>여러 DVI (다른 discrepancy) 를 비교합니다.</li>
        </ul>
      </li>
      <li>일부 discrepancy 는
        <ul>
          <li>두 모드 사이의 어중간한 곳에 mass 를 두거나,</li>
          <li>한 모드만 과도하게 살리는 등,
직관적인 “좋은 parameter” 와는 동떨어진 posterior 를 만듭니다.</li>
        </ul>
      </li>
      <li>저자들의 메시지는 다음과 같습니다.
        <blockquote>
          <p>단순히 divergence 를 바꾸는 것만으로<br />
항상 “좋은 approximate posterior” 를 얻을 수 있다고 믿는 것은 위험하다.</p>
        </blockquote>
      </li>
    </ul>
  </li>
</ol>

<p>이에 반해 GVI 는 RoT/axioms 를 따르기 때문에</p>

<ul>
  <li>$\ell$ 과 $D$ 의 역할이 명확히 분리되고 (modular),</li>
  <li>$D$ 를 바꾸더라도 “최선의 $\theta$” 는 동일하게 유지한 채,</li>
  <li><strong>불확실성의 크기와 모양만 조정</strong>하는 것이 가능하다고 주장합니다.</li>
</ul>

<hr />

<h3 id="black-box-gvi-bbgvi">Black-box GVI (BBGVI)</h3>

<p>실제 계산 관점에서 보면, GVI objective 는</p>

\[\mathcal J(q)
=
\mathbb E_q\bigg[\sum_{i=1}^n \ell(\theta, x_i)\bigg]
+ D(q \,\|\, \pi)\]

<p>이며, 우리는 $q(\theta)$ 를 어떤 파라미터 $\kappa$ 를 가진 분포 $q_\kappa(\theta)$ 로 표현합니다.</p>

<p>대부분의 경우 위 기대값과 regularization 항은 <strong>analytic 하지 않기 때문에</strong>,<br />
논문에서는 <strong>Black-box GVI (BBGVI)</strong> 라는 이름으로 일반적인 최적화 스킴을 제안합니다.</p>

<p>아이디어는 standard black-box VI 와 거의 동일합니다.</p>

<ol>
  <li><strong>Variational family 파라미터화</strong>
    <ul>
      <li>$q_\kappa(\theta)$ 의 예시:
        <ul>
          <li>Mean-field Gaussian: $\kappa = (\mu, \log \sigma)$</li>
          <li>Normalizing flow: base Gaussian + invertible transform</li>
          <li>BNN: 네트워크 weight 에 대한 factorized Gaussian 등</li>
        </ul>
      </li>
    </ul>
  </li>
  <li><strong>Reparameterization / 샘플링</strong>
    <ul>
      <li>고정된 noise $\epsilon \sim p(\epsilon)$ 를 샘플링하고,</li>
      <li>$\theta = g(\epsilon; \kappa)$ 로 변환하여 $\theta \sim q_\kappa$ 를 구현합니다.</li>
    </ul>
  </li>
  <li><strong>Monte Carlo 로 objective 및 gradient 근사</strong>
    <ul>
      <li>샘플 ${\theta^{(s)}}$ 에 대해
$$
\widehat{\mathcal J}(\kappa)
=
\frac{1}{S}\sum_{s=1}^S
\bigg[
  \sum_{i=1}^n \ell(\theta^{(s)}, x_i)
\bigg]
        <ul>
          <li>\widehat{D}(q_\kappa \,|\, \pi)
$$</li>
        </ul>
      </li>
      <li>$D$ 역시 analytic form 이 없으면 Monte Carlo / score function / reparam gradient 등으로 근사합니다.</li>
      <li>reparameterization trick 덕분에<br />
$\nabla_\kappa \widehat{\mathcal J}(\kappa)$ 는 backprop 으로 계산할 수 있습니다.</li>
    </ul>
  </li>
  <li><strong>Stochastic optimization</strong>
    <ul>
      <li>미니배치 $x_{1:m}$ 을 사용하면 SVI 스타일로 확장할 수 있습니다.</li>
      <li>Adam / SGD 등을 사용해<br />
$\kappa \leftarrow \kappa - \eta \nabla_\kappa \widehat{\mathcal J}(\kappa)$ 로 업데이트합니다.</li>
    </ul>
  </li>
</ol>

<p>Standard VI (ELBO, NLL + KL) 도 이 틀의 특수 케이스이므로,<br />
기존 black-box VI 코드가 있다면 <strong>loss $\ell$ 과 divergence $D$ 만 바꾸어 GVI 실험</strong>을 수행할 수 있다는 점을 강조합니다.</p>

<hr />

<h3 id="applications-bnn-deep-gp-에서의-gvi">Applications: BNN, Deep GP 에서의 GVI</h3>

<p>논문 후반부에서는 GVI 를 실제 모델에 적용한 실험을 보여줍니다. (간단 요약)</p>

<ol>
  <li><strong>Bayesian Neural Networks (BNN)</strong>
    <ul>
      <li>Standard VI (NLL + KL, mean-field) 와<br />
GVI (robust loss / tempered divergence 등) 를 비교합니다.</li>
      <li>heavy-tailed noise, outlier 가 많은 상황에서
        <ul>
          <li>standard VI 는 overconfident / miscalibrated posterior 를 생성하는 반면,</li>
          <li>GVI 는 분산을 더 적절히 키우고, 예측 calibration 이 개선되는 모습을 보입니다.</li>
        </ul>
      </li>
    </ul>
  </li>
  <li><strong>Deep Gaussian Processes (Deep GP)</strong>
    <ul>
      <li>generalized loss + divergence 를 넣은 GVI 를 적용합니다.</li>
      <li>Noise / model misspecification 세팅에서<br />
GVI 가 standard VI 보다 더 robust 한 predictive performance 를 보인다고 보고합니다.</li>
    </ul>
  </li>
</ol>

<p>이 실험들은 “loss 와 divergence 라는 두 개의 다이얼을 돌려서,<br />
Bayesian 추론의 <strong>robustness / calibration / misspecification 대응력</strong>을 조절할 수 있다”는 주장의 정성적 근거로 등장합니다.</p>

<hr />

<h2 id="discussion--takeaways">Discussion &amp; Takeaways</h2>

<p>마지막으로, 이 논문을 정리하면 다음과 같습니다.</p>

<ol>
  <li><strong>Bayes 를 “최적화 문제”로 이해하자.</strong>
    <ul>
      <li>Bayes posterior 는
\(\min_q \big\{ \mathbb E_q[\text{loss}] + \mathrm{KL}(q \,\|\, \pi) \big\}\)
라는 무한 차원 최적화 문제의 해입니다.</li>
      <li>이 관점은 VI, EM, generalized Bayes, PVI 등을 한 프레임에서 비교·확장하는 데 매우 유용합니다.</li>
    </ul>
  </li>
  <li><strong>Standard VI(KL + ELBO) 는, 주어진 family 안에서는 “정석”이다.</strong>
    <ul>
      <li>Variational family $\mathcal Q$ 가 고정되어 있을 때,<br />
KL 기반 VI 는 Bayes-as-optimization 을 $\mathcal Q$ 로 제한한 문제의 최적해입니다.</li>
      <li>같은 $\mathcal Q$ 에서 다른 divergence 기반 VI(DVI)를 사용할 경우,<br />
Bayesian optimality 관점에서 “왜 그것이 자연스러운 선택인지”를 추가로 설명해야 합니다.</li>
    </ul>
  </li>
  <li><strong>RoT: 세 개의 다이얼로 posterior 를 디자인하는 프레임워크</strong>
    <ul>
      <li>Loss $\ell$ : 어떤 파라미터가 좋은지 (fit / scoring rule)</li>
      <li>Divergence $D$ : prior 대비 얼마나 멀어질 수 있는지, 불확실성의 크기/모양</li>
      <li>Feasible set $\Pi$ : 계산·표현 측면에서의 제약 (full, variational, flow, …)</li>
    </ul>

    <p>→ 이 세 가지를 동시에 튜닝하면서 “새로운 posterior” 를 설계하는 통일된 언어를 제공합니다.</p>
  </li>
  <li><strong>GVI: RoT 를 실제로 쓰기 위한 variational 버전</strong>
    <ul>
      <li>$\Pi = \mathcal Q$ 로 제한하여 tractable 하게 만들고,</li>
      <li>Black-box 최적화(SVI, reparam, MC gradient) 를 사용해 구현할 수 있습니다.</li>
      <li>RoT 의 공리를 계승하므로, loss 와 regularizer 의 역할이 modular 하게 유지된다는 장점이 있습니다.</li>
    </ul>
  </li>
  <li><strong>PVI / scoring-rule 기반 VI 와의 연결</strong>
    <ul>
      <li>우리가 관심 있는 <strong>Predictive Variational Inference (PVI)</strong> 는<br />
RoT/GVI 에서 보면 “loss $\ell$ 을 log-score 대신 다른 proper scoring rule 로 바꾸는 조합”으로 자연스럽게 해석할 수 있습니다.</li>
      <li>즉, 이 논문은 PVI 를<br />
“Bayes-as-optimization 의 한 generalization” 으로 어디에 위치시킬지 설명해 주는 이론적 배경이 됩니다.</li>
    </ul>
  </li>
</ol>]]></content><author><name>Junhee Kim</name></author><category term="Statistics" /><category term="Bayesian" /><category term="Machine Learning" /><category term="Generalized Variational Inference" /><category term="Bayesian Deep Learning" /><summary type="html"><![CDATA[이 포스팅은 Knoblauch, Jewson, Damoulas (2019) 의 “Generalized Variational Inference: Three arguments for deriving new posteriors” 를 읽고 정리한 글입니다.]]></summary></entry><entry><title type="html">Variational Inference</title><link href="https://joon0390.github.io/variational-inference/" rel="alternate" type="text/html" title="Variational Inference" /><published>2025-12-01T00:00:00+09:00</published><updated>2025-12-01T00:00:00+09:00</updated><id>https://joon0390.github.io/VI</id><content type="html" xml:base="https://joon0390.github.io/variational-inference/"><![CDATA[<blockquote>
  <p>이 포스팅은 <strong>Variational Inference(VI)</strong> 를 정리한 글입니다.</p>
</blockquote>

<hr />

<h2 id="introduction">Introduction</h2>

<p>베이지안 추론의 목표는 관측 데이터 $y$ 에 대해 <strong>사후분포(posterior)</strong></p>

\[p(\theta \mid y)
\propto
p(\theta)\, p(y \mid \theta)\]

<p>를 계산하는 것입니다.</p>

<p>하지만 실제로는</p>

<ul>
  <li>복잡한 likelihood</li>
  <li>고차원 파라미터</li>
  <li>비선형/비가우시안 모델</li>
</ul>

<p>때문에 정규화 상수인 <strong>evidence</strong></p>

\[p(y) = \int p(\theta)\,p(y\mid\theta)\,d\theta\]

<p>를 닫힌형태로 구할 수 없는 경우가 대부분입니다.</p>

<p>전통적으로는</p>

<ul>
  <li>MCMC, SMC, Particle Filter 등 <strong>샘플링 기반 방법</strong></li>
  <li>Laplace approximation, EP 등 <strong>근사 방법</strong></li>
</ul>

<p>이 사용되어 왔고, 그 중에서 <strong>Variational Inference(VI)</strong> 는</p>

<blockquote>
  <p>“샘플링 문제를 <strong>최적화 문제(Optimization)</strong>로 바꿔서 푸는 근사 베이지안 추론 방법”</p>
</blockquote>

<p>으로 이해할 수 있습니다.</p>

<hr />

<h2 id="variational-inference의-핵심-아이디어">Variational Inference의 핵심 아이디어</h2>

<p>아이디어는 아주 간단합니다.</p>

<ol>
  <li><strong>복잡한 posterior $p(\theta\mid y)$ 대신</strong></li>
  <li><strong>“계산 가능한” 분포족 $\mathcal{Q}$ 안에서 하나의 분포 $q(\theta)$를 골라</strong></li>
  <li><strong>$p(\theta\mid y)$ 와 최대한 비슷하게 만들자.</strong></li>
</ol>

<p>즉,</p>

<blockquote>
  <p>posterior를 직접 샘플링하지 않고,<br />
“<strong>잘 다룰 수 있는 분포 $q(\theta)$</strong>”로 근사하자.</p>
</blockquote>

<p>이를 위해 <strong>KL divergence</strong> 를 사용합니다.</p>

<p><del>생성모델로 변분추론에 대한 개념도를 쉽게 그려달라고 해보았습니다.ㅎㅎ</del>
<img src="/assets/img/vi/vi_gpt.png" alt="variational inference" /></p>

<hr />

<h2 id="kl-최소화-관점">KL 최소화 관점</h2>

<p>우리가 하고 싶은 것은</p>

\[q^\star(\theta)
=
\arg\min_{q \in \mathcal{Q}}
\text{KL}\big(q(\theta) \,\|\, p(\theta\mid y)\big)\]

<p>입니다.</p>

<p>여기서 KL divergence는</p>

\[\text{KL}\big(q \,\|\, p\big)
=
\mathbb{E}_q \big[
\log q(\theta) - \log p(\theta)
\big]\]

<p>로 정의됩니다.</p>

<p>Posterior에 대해서 쓰면</p>

\[\text{KL}\big(q(\theta)\,\|\, p(\theta\mid y)\big)
=
\mathbb{E}_q \big[
\log q(\theta) - \log p(\theta\mid y)
\big].\]

<p>Bayes 공식 $p(\theta\mid y) = p(y,\theta)/p(y)$ 를 대입하면
\(\begin{align}
\text{KL}\big(q \,\|\, p(\theta\mid y)\big)
&amp;=
\mathbb{E}_q \big[
\log q(\theta) - \log p(y,\theta) + \log p(y)
\big] \\
&amp;=
\underbrace{
\mathbb{E}_q[\log q(\theta)] - \mathbb{E}_q[\log p(y,\theta)]
}_{\text{(1)}}
+
\log p(y).
\end{align}\)
여기서 $\log p(y)$ 는 $\theta$와 무관한 상수입니다.</p>

<p>따라서</p>

<ul>
  <li>KL을 최소화하는 대신</li>
  <li>아래 항을 <strong>최대화</strong>해도 동일한 해를 얻습니다.</li>
</ul>

\[\mathcal{L}(q)
=
\mathbb{E}_q[\log p(y,\theta)]
-
\mathbb{E}_q[\log q(\theta)].\]

<p>이 $\mathcal{L}(q)$ 를 <strong>Evidence Lower Bound (ELBO)</strong> 라고 부릅니다.</p>

<hr />

<h2 id="elbo-evidence-lower-bound">ELBO: Evidence Lower Bound</h2>

<p>위 식을 정리하면,</p>

\[\log p(y)
=
\mathcal{L}(q)
+
\text{KL}\big(q(\theta)\,\|\,p(\theta\mid y)\big).\]

<ul>
  <li>$\log p(y)$ : 고정된 상수 (모델 evidence)</li>
  <li>KL ≥ 0 이므로</li>
</ul>

\[\mathcal{L}(q)
\le
\log p(y).\]

<p>따라서 ELBO는 이름 그대로 <strong>evidence에 대한 하한(lower bound)</strong> 입니다.</p>

<p>또한, KL 최소화와 ELBO 최대화가 동치이므로</p>

\[q^\star(\theta)
=
\arg\max_{q\in\mathcal{Q}} \mathcal{L}(q).\]

<p>즉,</p>

<blockquote>
  <p>VI는 “<strong>ELBO 최대화 문제</strong>”를 푸는 것과 완전히 같게 됩니다.</p>
</blockquote>

<hr />

<h2 id="interpretaion-of-elbo">Interpretaion of ELBO</h2>

<p>ELBO는 두 항으로 분해됩니다.
\(\begin{align}
\mathcal{L}(q)
&amp;=
\mathbb{E}_q[\log p(y,\theta)]
-
\mathbb{E}_q[\log q(\theta)] \\
&amp;=
\mathbb{E}_q[\log p(y\mid \theta)]
+
\mathbb{E}_q[\log p(\theta)]
-
\mathbb{E}_q[\log q(\theta)].
\end{align}\)
이를 해석하면</p>

<ul>
  <li>$\mathbb{E}_q[\log p(y\mid\theta)]$ : <strong>expected log-likelihood</strong><br />
→ 데이터 적합도(fit)</li>
  <li>$-\text{KL}(q(\theta)\,|\,p(\theta))$<br />
→ prior와 q 사이의 거리 (regularization 역할)</li>
</ul>

<p>즉 ELBO는</p>

<blockquote>
  <p>“데이터 적합도 + prior regularization − 엔트로피 패널티”</p>
</blockquote>

<p>로 해석할 수 있습니다.</p>

<hr />

<h2 id="variational-family">Variational Family</h2>

<p>VI의 성능은 <strong>어떤 family $\mathcal{Q}$를 선택하느냐</strong>에 강하게 의존합니다.</p>

<p>대표적인 선택은 다음과 같습니다.</p>

<ol>
  <li>
    <p><strong>Mean-field family</strong></p>

\[q(\theta) = \prod_{j=1}^d q_j(\theta_j)\]

    <ul>
      <li>각 파라미터/블록이 서로 독립이라고 가정</li>
      <li>계산이 매우 단순해짐 (coordinate ascent 가능)</li>
      <li>하지만 <strong>의존구조(correlation)를 표현하지 못함</strong></li>
    </ul>
  </li>
  <li>
    <p><strong>Full-covariance Gaussian</strong></p>

\[q(\theta) = \mathcal{N}(\mu, \Sigma)\]

    <ul>
      <li>공분산까지 학습하여 상관관계 표현 가능</li>
      <li>파라미터 수가 $O(d^2)$ 로 증가</li>
    </ul>
  </li>
  <li>
    <p><strong>Mixture / Normalizing flow 등 리치한 family</strong></p>

    <ul>
      <li>Mixture of Gaussians</li>
      <li>Normalizing Flows 기반 $q(\theta)$</li>
      <li>매우 표현력이 높지만 최적화가 더 어려움</li>
    </ul>
  </li>
</ol>

<p>이 글에서는 가장 기본이 되는 <strong>mean-field VI</strong> 를 중심으로 설명하겠습니다.</p>

<hr />

<h2 id="mean-field-vi와-coordinate-ascent">Mean-field VI와 Coordinate Ascent</h2>

<p>Mean-field 가정:</p>

\[q(\theta) = \prod_{j=1}^M q_j(\theta_j).\]

<p>이때, ELBO는</p>

\[\mathcal{L}(q_1,\dots,q_M)
=
\mathbb{E}_{q}[\log p(y,\theta)]
-
\sum_{j=1}^M \mathbb{E}_{q_j}[\log q_j(\theta_j)].\]

<p>여기서 중요한 것은:</p>

<blockquote>
  <p>나머지를 고정하고 <strong>하나의 factor $q_j$만 최적화</strong> 하면,<br />
닫힌형태의 update 식을 얻을 수 있다.</p>
</blockquote>

<p>정확히는,</p>

\[\log q_j^\star(\theta_j)
=
\mathbb{E}_{q_{-j}}[\log p(y,\theta)]
+ \text{const},\]

<p>여기서 $q_{-j}$ 는 $j$번째를 제외한 모든 factor의 곱입니다.</p>

<p>즉,</p>

<blockquote>
  <p><strong>“joint log density를 기대값 취해서 exponentiation → 정규화”</strong><br />
를 반복하면 된다.</p>
</blockquote>

<h3 id="알고리즘-cavi-coordinate-ascent-vi">알고리즘 (CAVI: Coordinate Ascent VI)</h3>

<ol>
  <li>초기화: $q_j^{(0)}(\theta_j)$ 적당히 설정</li>
  <li>
    <p>반복 (j = 1,…,M 순회)</p>

\[\log q_j^{\text{new}}(\theta_j)
\propto
\mathbb{E}_{q_{-j}^{\text{old}}}[\log p(y,\theta)].\]
  </li>
  <li>모든 $j$ 에 대해 업데이트 후, ELBO 수렴 시까지 반복</li>
</ol>

<p>이 방식은 <strong>conjugate exponential family</strong> 구조에서 closed-form update를 제공하여,</p>

<ul>
  <li>LDA</li>
  <li>Bayesian mixture model</li>
  <li>Factor analysis</li>
</ul>

<p>등 여러 모델에 대해 <strong>“논문 한 편 = CAVI 업데이트 유도”</strong> 가 되는 경우가 많습니다.</p>

<hr />

<h2 id="exponential-family와-conjugacy">Exponential Family와 Conjugacy</h2>

<p>만약 joint 분포 $p(y,\theta)$ 가 <strong>exponential family</strong> 구조를 가지고 있고, prior–likelihood가 conjugate 하다면,</p>

<p>각 factor $q_j(\theta_j)$ 역시 동일한 family에 속하게 됩니다.</p>

<p>예를 들어,</p>

<ul>
  <li>Likelihood: $p(y\mid\theta)$ 는 Gaussian</li>
  <li>Prior: $p(\theta)$ 도 Gaussian</li>
  <li>Posterior: $p(\theta\mid y)$ 도 Gaussian (conjugate)</li>
</ul>

<p>이때 Mean-field VI를 적용하면,</p>

<ul>
  <li>$q(\theta)$도 Gaussian</li>
  <li>update는 “posterior의 natural parameter에 expectation만 집어넣은 형태”로 표현</li>
</ul>

<p>→ 완전히 analytic update가 가능해집니다.</p>

<hr />

<h2 id="stochastic-variational-inference-svi">Stochastic Variational Inference (SVI)</h2>

<p>문제는:</p>

<ul>
  <li>데이터 $y = (y_1,\dots,y_N)$ 가 매우 클 때</li>
  <li>매 step마다 $\mathbb{E}_q[\log p(y,\theta)]$ 를 계산하는 것은 비현실적</li>
</ul>

<p>→ <strong>mini-batch 기반 stochastic gradient</strong> 를 사용하자.</p>

<p>SVI는</p>

<ol>
  <li>ELBO를 <strong>stochastic gradient ascent</strong> 로 최적화</li>
  <li>각 step에서 mini-batch 데이터만 사용</li>
  <li>learning rate $\rho_t$ 를 줄여가며 수렴 보장</li>
</ol>

<h3 id="score-function-gradient-reinforce">Score Function Gradient (REINFORCE)</h3>

<p>ELBO를 $\mathcal{L}(\lambda)$, $q(\theta;\lambda)$ 라 쓰면
$$
\begin{align}
\mathcal{L}(\lambda)
&amp;=
\mathbb{E}<em>{q(\theta;\lambda)}[\log p(y,\theta)]
-
\mathbb{E}</em>{q(\theta;\lambda)}[\log q(\theta;\lambda)].
\end{align}</p>

<p>score function trick을 쓰면,</p>

<p>\begin{align}
\nabla_\lambda \mathcal{L}(\lambda)
&amp;=
\mathbb{E}<em>{q(\theta;\lambda)}
\big[
\nabla</em>\lambda \log q(\theta;\lambda)
\big(
\log p(y,\theta) - \log q(\theta;\lambda)
\big)
\big].
\end{align}
$$
이를 Monte Carlo로 근사하면,</p>

<ol>
  <li>$\theta^{(s)} \sim q(\theta;\lambda)$</li>
  <li></li>
</ol>

\[\hat{g}
   =
   \frac{1}{S} \sum_{s=1}^S
   \nabla_\lambda \log q(\theta^{(s)};\lambda)
   \big(
   \log p(y,\theta^{(s)}) - \log q(\theta^{(s)};\lambda)
   \big).\]

<ol>
  <li>$\lambda \leftarrow \lambda + \rho_t \hat{g}$</li>
</ol>

<p>하지만 이 gradient는 <strong>분산이 매우 크다</strong>는 단점이 있습니다.</p>

<hr />

<h2 id="reparameterization-trick">Reparameterization Trick</h2>

<p>최근 딥러닝에서 쓰는 VI(예: VAE)는 대부분 <strong>reparameterization gradient</strong> 를 사용합니다.</p>

<p>아이디어:</p>

<ul>
  <li>$q(\theta;\lambda)$에서 직접 샘플하지 말고</li>
  <li><strong>고정 분포 $\epsilon \sim p(\epsilon)$</strong> 에서 샘플한 뒤</li>
  <li>$\theta = g(\epsilon;\lambda)$ 로 변환해서 얻기</li>
</ul>

<p>예: Gaussian $q(\theta;\mu,\sigma^2)$</p>

<ul>
  <li>$\epsilon \sim \mathcal{N}(0,1)$</li>
  <li>$\theta = \mu + \sigma \epsilon$</li>
</ul>

<p>그러면</p>

\[\mathcal{L}(\lambda)
=
\mathbb{E}_{\epsilon\sim p(\epsilon)}
\big[
\log p(y, g(\epsilon;\lambda)) - \log q(g(\epsilon;\lambda);\lambda)
\big].\]

<p>이제 gradient는
\(\begin{align}
\nabla_\lambda \mathcal{L}(\lambda)
&amp;=
\mathbb{E}_{\epsilon}
\big[
\nabla_\lambda
\big(
\log p\big(y, g(\epsilon;\lambda)\big)
-
\log q\big(g(\epsilon;\lambda);\lambda\big)
\big)
\big].
\end{align}\)</p>

<ul>
  <li>$\epsilon$ 은 $\lambda$ 와 무관</li>
  <li>안쪽의 gradient는 일반적인 backprop으로 계산 가능</li>
  <li>분산이 작은 gradient estimator 확보</li>
</ul>

<p>그래서 VAE, Bayesian neural net 등에서는</p>

<ul>
  <li>encoder $q_\phi(z\mid x)$</li>
  <li>decoder $p_\theta(x\mid z)$</li>
</ul>

<p>를 두고, reparameterization trick + SGD로 ELBO를 최적화합니다.</p>

<hr />

<h2 id="vi-vs-mcmc--smc">VI vs MCMC / SMC</h2>

<p>간단히 요약하면:</p>

<table>
  <thead>
    <tr>
      <th>방법</th>
      <th>핵심 아이디어</th>
      <th>장점</th>
      <th>단점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>MCMC</td>
      <td>Markov chain으로 posterior 샘플링</td>
      <td>이론적으로 정확 (시간→∞)</td>
      <td>느릴 수 있음, mixing 문제</td>
    </tr>
    <tr>
      <td>SMC / PF</td>
      <td>Sequential하게 importance sampling + resampling</td>
      <td>online, non-linear/non-Gaussian state space에 강함</td>
      <td>high-dim에서 particle 수 폭발</td>
    </tr>
    <tr>
      <td>VI</td>
      <td>최적화를 통해 근사 posterior 학습</td>
      <td>빠름, 대규모 데이터에 좋음</td>
      <td>mode-seeking, uncertainty 과소추정 가능</td>
    </tr>
  </tbody>
</table>

<p>특히 VI는</p>

<ul>
  <li><strong>KL$(q|p)$</strong> 를 최소화하기 때문에</li>
  <li>posterior의 <strong>tail / 작은 모드</strong> 를 무시하고</li>
  <li><strong>주요 모드 근처에 mass를 집중</strong>시키는 경향(mode-seeking)이 있습니다.</li>
</ul>

<p>이 부분은 나중에 <strong>Predictive VI / Proper Scoring Rule 기반 VI</strong> 등으로 보완할 수 있습니다.</p>

<hr />

<h2 id="예시-bayesian-logistic-regression에서의-vi">예시: Bayesian Logistic Regression에서의 VI</h2>

<p>간단한 예로, Bayesian logistic regression을 생각해봅시다.</p>

<ul>
  <li>데이터: $(x_i,y_i)$, $y_i\in{0,1}$</li>
  <li>파라미터: $\beta\in\mathbb{R}^p$</li>
</ul>

<p>모델:
\(\begin{align}
y_i \mid x_i,\beta &amp;\sim \text{Bernoulli}(\sigma(x_i^\top\beta)), \\
\beta &amp;\sim \mathcal{N}(0,\alpha^{-1}I).
\end{align}\)</p>

<ul>
  <li>posterior $p(\beta\mid y)$ 는 closed form 없음</li>
  <li>MCMC도 가능하지만, 큰 데이터에서는 느릴 수 있음</li>
</ul>

<p>여기서 VI:</p>

<ul>
  <li>$q(\beta)$ 를 Gaussian $\mathcal{N}(m,S)$ 로 가정</li>
  <li>
    <p>ELBO:</p>

\[\mathcal{L}(m,S)
=
\mathbb{E}_q[\log p(y\mid\beta)]
+
\mathbb{E}_q[\log p(\beta)]
-
\mathbb{E}_q[\log q(\beta)].\]
  </li>
  <li>$\mathbb{E}_q[\log p(y\mid\beta)]$ 는 analytic하지 않지만<br />
→ Monte Carlo + reparameterization 로 gradient 구해서<br />
→ $(m,S)$ 에 대해 SGD 수행</li>
</ul>

<p>이 구조가 Bayesian deep learning, VAE 등으로 그대로 확장됩니다.</p>

<hr />

<h2 id="vi는-언제-좋고-언제-나빠질까">VI는 언제 좋고, 언제 나빠질까</h2>

<p><strong>좋은 경우</strong></p>

<ul>
  <li>posterior가 거의 unimodal</li>
  <li>mean-field 근사로도 크게 정보 손실이 없는 경우</li>
  <li>아주 큰 데이터셋, deep model처럼 MCMC가 비현실적인 경우</li>
</ul>

<p><strong>나빠지는 경우</strong></p>

<ul>
  <li>posterior가 <strong>강하게 multi-modal</strong></li>
  <li>변수 간 <strong>강한 상관관계</strong>가 있음에도 factorized family 사용</li>
  <li>model misspecification 하에서 <strong>uncertainty를 과소추정</strong>하는 경우</li>
</ul>

<hr />

<h2 id="summary">Summary:</h2>

<ul>
  <li>VI는 posterior 추론을 <strong>최적화 문제(ELBO 최대화)</strong> 로 바꾸는 방법이다.</li>
  <li>Mean-field VI는 계산이 간단하지만 상관구조를 잃기 쉽다.</li>
  <li>Stochastic VI + reparameterization은 딥러닝에서는 사실상 표준이다.</li>
  <li>KL$(q|p)$ 특성 때문에 <strong>mode-seeking + 과도한 확신</strong>이라는 한계가 있다.</li>
</ul>

<hr />

<h2 id="reference">Reference</h2>

<ul>
  <li><a href="https://arxiv.org/pdf/1601.00670">David M. B (2018). Variational Inference: A Review for Statistician</a></li>
  <li><a href="https://www.jmlr.org/papers/volume14/hoffman13a/hoffman13a.pdf">Matthew D. H (2013). Stochastic Variational Inference</a></li>
  <li><a href="https://www.tandfonline.com/doi/full/10.1080/01621459.2017.1285773">David M. B (2017). Variational Inference: A Review for Statisticians</a></li>
</ul>]]></content><author><name>Junhee Kim</name></author><category term="Statistics" /><category term="Bayesian" /><category term="Machine Learning" /><category term="Variational Inference" /><category term="Bayesian Deep Learning" /><summary type="html"><![CDATA[이 포스팅은 Variational Inference(VI) 를 정리한 글입니다.]]></summary></entry><entry><title type="html">Particle Filter : Sequential Monte Carlo</title><link href="https://joon0390.github.io/particle-filter/" rel="alternate" type="text/html" title="Particle Filter : Sequential Monte Carlo" /><published>2025-11-14T00:00:00+09:00</published><updated>2025-11-14T00:00:00+09:00</updated><id>https://joon0390.github.io/ParticleFilter</id><content type="html" xml:base="https://joon0390.github.io/particle-filter/"><![CDATA[<blockquote>
  <p>이 포스팅은 <strong>Sequential Monte Carlo(SMC)</strong> 방법의 대표 알고리즘인 <strong>Particle Filter</strong> 의 핵심 개념과 구조를 정리한 글입니다.</p>
</blockquote>

<blockquote>
  <p><a href="/filtering/">“Filtering &amp; State Space Model”</a> 에 이어지는 내용입니다.</p>
</blockquote>

<hr />

<h2 id="introduction">Introduction</h2>

<p>앞선 글에서는 Filtering이 무엇인지,<br />
그리고 선형·정규성 가정하에서 Kalman Filter가 어떻게 closed-form 해를 제공하는지 정리했었습니다.</p>

<p>하지만 대부분의 실제 시스템은</p>

<ul>
  <li><strong>비선형(nonlinear)</strong></li>
  <li><strong>비가우시안(non-Gaussian)</strong></li>
  <li><strong>multi-modal posterior</strong></li>
  <li><strong>outlier 포함</strong></li>
  <li><strong>복잡한 likelihood</strong></li>
</ul>

<p>을 가지기 때문에 칼만 계열(EKF/UKF 포함)만으로는 정확한 추정이 어렵습니다.</p>

<p>이 문제를 해결하기 위한 가장 일반적인 방법이 바로 <strong>Particle Filter(입자 필터)</strong> 입니다.</p>

<p>Particle Filter는 <strong>posterior 분포 자체를 Monte Carlo method로 직접 근사하는 방법</strong>이며, 필터링 문제에 대한 <strong>가장 유연한 범용 해법</strong>이라고 할 수 있습니다.</p>

<h2 id="particle-filter의-핵심-아이디어">Particle Filter의 핵심 아이디어</h2>

<p>Particle Filter는 posterior 분포 $p(x_t \mid y_{1:t})$를 <strong>일련의 샘플(particles)</strong> 과 <strong>가중치(weights)</strong> 의 조합으로 근사합니다.</p>

<p>즉,</p>

\[p(x_t \mid y_{1:t}) \approx
\sum_{i=1}^N w_t^{(i)} \, \delta(x_t - x_t^{(i)})\]

<p>여기서</p>

<ul>
  <li>$x_t^{(i)}$: i번째 particle</li>
  <li>$w_t^{(i)}$: 해당 particle의 중요도 가중치</li>
  <li>$\delta(\cdot)$: Dirac delta (point mass)</li>
</ul>

<p>즉, posterior라는 복잡한 분포를 “<strong>여러 개의 Weighted Samples</strong>” 로 나타내는 것입니다.</p>

<p>핵심은 다음과 같습니다.</p>

<blockquote>
  <p><strong>비선형이든 비정규성이든 상관없이, 샘플만 잘 뿌리면 어떤 분포든 근사할 수 있다.</strong></p>
</blockquote>

<p>이런 관점에서 PF는 매우 강력합니다.</p>

<h2 id="sir-particle-filter-bootstrap-filter">SIR Particle Filter (Bootstrap Filter)</h2>

<p>가장 기본적이면서 널리 쓰이는 PF는 <strong>SIR (Sampling–Importance–Resampling)</strong> 입니다.</p>

<p>각 단계는 다음과 같습니다.</p>

<h2 id="1-initialization">1) Initialization</h2>

<p>초기 prior로부터 particle를 샘플링:</p>

\[x_0^{(i)} \sim p(x_0), \qquad w_0^{(i)} = \frac{1}{N}\]

<h2 id="2-prediction-sampling">2) Prediction (Sampling)</h2>

<p>각 particle을 transition model에 따라 다음 시점으로 이동시킴:</p>

\[x_t^{(i)} \sim p(x_t \mid x_{t-1}^{(i)})\]

<p>이는 filtering 공식의 <strong>Prediction 단계</strong>에 해당합니다.</p>

<h2 id="3-update-importance-weighting">3) Update (Importance Weighting)</h2>

<p>새로운 관측값 $y_t$가 주어지면, likelihood로 가중치를 업데이트합니다.</p>

\[w_t^{(i)} \propto p(y_t \mid x_t^{(i)})\]

<p>정규화:</p>

\[w_t^{(i)} =
\frac{w_t^{(i)}}{\sum_{j=1}^N w_t^{(j)}}\]

<p>여기까지가 filtering 공식의 <strong>Update 단계</strong>입니다.</p>

<h2 id="4-resampling-필수-단계">4) Resampling (필수 단계)</h2>

<p>문제는, 시간이 지나면 대부분의 weight가 0이 되고<br />
몇 개의 particle만 큰 weight를 가지는 <strong>degeneracy issue</strong>가 발생합니다.</p>

<p>이를 해결하기 위해:</p>

<ul>
  <li>weight가 큰 particle 중심으로 다시 샘플링하고</li>
  <li>weight를 모두 동일하게 만들어줍니다.</li>
</ul>

<p>Resampling의 주요 방식:</p>

<ul>
  <li>Multinomial</li>
  <li>Systematic</li>
  <li>Stratified</li>
</ul>

<h2 id="효율성-판단-기준-ess-effective-sample-size">효율성 판단 기준: ESS (Effective Sample Size)</h2>

<p>가중치가 얼마나 collapse 되었는지 확인하기 위해:</p>

\[\text{ESS} = \frac{1}{\sum_{i=1}^N (w_t^{(i)})^2}\]

<ul>
  <li>ESS가 작아지면 → 대부분 weight가 0 → resampling 필요</li>
  <li>실전에서는 보통<br />
$\text{ESS} &lt; N/2$ 또는 $N/3$ 를 기준으로 사용</li>
</ul>

<h1 id="pf는-importance-sampling-기반-방법이다">PF는 Importance Sampling 기반 방법이다</h1>

<p>PF는 사실상 <strong>Sequential Importance Sampling(SIS)</strong> 과 필수적으로 따라오는 <strong>Resampling(SIR)</strong> 의 조합입니다.</p>

<p>즉,</p>

<ul>
  <li>proposal distribution: transition model</li>
  <li>target distribution: filtering posterior</li>
  <li>importance weight: likelihood</li>
</ul>

<p>이 구조는 MCMC와도 유사한 probabilistic intuition을 제공하기 때문에, PF는 Bayesian computation 영역에서 매우 중요한 알고리즘입니다.</p>

<h1 id="왜-pf는-강력한가">왜 PF는 강력한가?</h1>

<p><strong>비선형 + 비가우시안 + multimodal posterior를 그대로 다룰 수 있기 때문입니다.</strong></p>

<p>만약 posterior가 아래처럼 두 개의 모드를 가진다면:</p>

<ul>
  <li>EKF: 망함 (선형화 + Gaussian 강제)</li>
  <li>UKF: 망함 (Gaussian 강제)</li>
  <li>PF: particle가 두 모드에 각각 분포하면 그대로 표현 가능</li>
</ul>

<p>또한 이상치(outlier)가 등장해 likelihood가 heavy-tailed일 때도 PF는 Monte Carlo 방식이기 때문에 자연스럽게 적응합니다.</p>

<h1 id="하지만-pf도-한계가-있다">하지만 PF도 한계가 있다</h1>

<p>특히:</p>

<h3 id="-sample-degeneracy">✔ Sample degeneracy</h3>
<p>weight collapse → resampling 필요<br />
너무 자주 resampling하면 diversity 감소</p>

<h3 id="-curse-of-dimensionality">✔ Curse of Dimensionality</h3>
<p>state dimension이 커지면 particle 수가 폭발적으로 필요</p>

<p>그래서 등장하는 여러 개선 알고리즘들이 있습니다.</p>

<h1 id="pf의-주요-변형들">PF의 주요 변형들</h1>

<h2 id="1-auxiliary-particle-filter-apf">1) Auxiliary Particle Filter (APF)</h2>

<ul>
  <li>관측 $y_t$ 정보를 미리 사용해 더 나은 proposal을 구성</li>
  <li>weight degeneracy를 줄이는 데 효과적</li>
</ul>

<h2 id="2-raoblackwellized-particle-filter-rbpf">2) Rao–Blackwellized Particle Filter (RBPF)</h2>

<p>PF로는 일부 state만 추적하고<br />
다른 state는 Kalman Filter로 처리하는 hybrid 방식.</p>

<p>예:<br />
SLAM에서 landmark map → KF<br />
robot pose → PF</p>

<h2 id="3-particle-learning-pl">3) Particle Learning (PL)</h2>

<p>posterior의 sufficient statistics를 업데이트하여<br />
파라미터 learning까지 동시에 수행.</p>

<h2 id="4-smc">4) SMC²</h2>

<p>parameter + latent state 둘 다 PF로 처리하는 확장.<br />
Bayesian inference 전체를 Sequential하게 수행.</p>

<h1 id="예시">예시</h1>

<p>단순한 비선형 시스템:</p>

\[x_t = x_{t-1} + 0.5\sin(x_{t-1}) + \epsilon_t,
\quad
y_t = \frac{x_t^2}{20} + \eta_t\]

<ul>
  <li>transition에 sine → 비선형</li>
  <li>observation에 square → 매우 비선형</li>
</ul>

<p>EKF/UKF는 대부분 실패하는 경우라고 볼 수 있습니다.</p>

<p>그러나 PF는 particle들이 분포의 모양을 그대로 따라가기 때문에 잘 작동합니다.</p>

<h1 id="code-example">Code Example</h1>

<p>아래 셀을 돌리면 1D Particle Filter가 동작합니다. <code>cfg</code>를 수정하며 센서 노이즈, outlier, 재샘플 방식을 실험할 수 있습니다.</p>

<div class="code-header">
    <button class="copy-code-button" title="Copy code to clipboard">
      <img class="copy-code-image" src="/assets/img/copy.png" />
    </button>
  </div>

<pre><code class="language-python">import numpy as np
import matplotlib.pyplot as plt

# Resampling
def multinomial_resample(w, rng):
    n = len(w)
    return rng.choice(n, size=n, p=w)

def systematic_resample(w, rng):
    n = len(w)
    positions = (np.arange(n) + rng.random()) / n
    idx, cdf = np.zeros(n, dtype=int), np.cumsum(w)
    i = j = 0
    while i &lt; n:
        if positions[i] &lt; cdf[j]:
            idx[i] = j; i += 1
        else:
            j += 1
    return idx

# Synthetic data
def make_synthetic(T=80, u=1.0, sigma_motion=0.4, sigma_obs=1.2,
                   outlier_prob=0.0, outlier_scale=8.0, seed=42):
    rng = np.random.default_rng(seed)
    x_true, z_obs, us = [], [], []
    x = 0.0
    for _ in range(T):
        x = x + u + rng.normal(0, sigma_motion)
        z = x + rng.normal(0, sigma_obs)
        if rng.random() &lt; outlier_prob:
            z += outlier_scale * rng.standard_normal()
        x_true.append(x); z_obs.append(z); us.append(u)
    return np.asarray(x_true), np.asarray(z_obs), np.asarray(us)

# Particle Filter
def particle_filter(zs, us, n=800, sigma_motion=0.4, sigma_obs=1.2,
                    rough_sigma=0.0, resample_thresh=0.5,
                    resample_method="systematic", seed=0):
    rng = np.random.default_rng(seed)
    xs = rng.normal(0, 5, size=n)
    w = np.ones(n) / n
    est, n_eff_hist = [], []
    resample_count = 0
    resampler = systematic_resample if resample_method == "systematic" else multinomial_resample

    for u, z in zip(us, zs):
        xs = xs + u + rng.normal(0, sigma_motion, size=n)  # Predict
        ll = np.exp(-0.5 * ((z - xs) / sigma_obs) ** 2) / (sigma_obs * np.sqrt(2 * np.pi))
        w *= ll; w += 1e-12; w /= w.sum()                  # Update

        n_eff = 1.0 / np.sum(w ** 2)
        n_eff_hist.append(n_eff)
        if n_eff &lt; resample_thresh * n:
            idx = resampler(w, rng)
            xs, w = xs[idx], np.ones(n) / n
            resample_count += 1
            if rough_sigma &gt; 0:
                xs += rng.normal(0, rough_sigma, size=n)

        est.append(xs.dot(w))

    return np.asarray(est), np.asarray(n_eff_hist), resample_count

# Config 
cfg = dict(
    T=80, u=1.0,
    sigma_motion=0.4, sigma_obs=1.2,
    outlier_prob=0.2, outlier_scale=6.0,
    particles=800, rough_sigma=0.1,
    resample_thresh=0.5, resample_method="systematic",
    seed=0,
)

x_true, z_obs, us = make_synthetic(
    T=cfg["T"], u=cfg["u"],
    sigma_motion=cfg["sigma_motion"], sigma_obs=cfg["sigma_obs"],
    outlier_prob=cfg["outlier_prob"], outlier_scale=cfg["outlier_scale"],
    seed=cfg["seed"],
)
est, n_eff_hist, resample_count = particle_filter(
    zs=z_obs, us=us, n=cfg["particles"],
    sigma_motion=cfg["sigma_motion"], sigma_obs=cfg["sigma_obs"],
    rough_sigma=cfg["rough_sigma"],
    resample_thresh=cfg["resample_thresh"],
    resample_method=cfg["resample_method"],
    seed=cfg["seed"],
)

rmse = float(np.sqrt(np.mean((est - x_true) ** 2)))
print(f"RMSE={rmse:.3f}, resample_times={resample_count}, "
      f"N_eff[min|median]={n_eff_hist.min():.1f}|{np.median(n_eff_hist):.1f}")

fig, (ax1, ax2) = plt.subplots(2, 1, figsize=(10, 6), sharex=True)
ax1.plot(x_true, label="true", linewidth=2)
ax1.plot(z_obs, ".", label="obs", alpha=0.6)
ax1.plot(est, label="PF estimate", linewidth=2)
ax1.legend(); ax1.grid(True, alpha=0.3); ax1.set_ylabel("position")
ax2.plot(n_eff_hist, label="N_eff", color="tab:orange")
ax2.axhline(cfg["resample_thresh"] * cfg["particles"], linestyle="--", color="gray", alpha=0.7)
ax2.legend(); ax2.grid(True, alpha=0.3); ax2.set_ylabel("N_eff"); ax2.set_xlabel("time")
plt.tight_layout(); plt.show()
</code></pre>

<ul>
  <li><code>outlier_prob</code>/<code>outlier_scale</code>로 센서 튐을 추가하거나 제거</li>
  <li><code>resample_method</code>을 <code>systematic</code> vs <code>multinomial</code>로 비교</li>
  <li><code>rough_sigma</code>로 재샘플 후 입자의 다양성 유지 효과를 확인</li>
</ul>]]></content><author><name>Junhee Kim</name></author><category term="Statistics" /><category term="Bayesian" /><category term="Time Series" /><category term="Machine Learning" /><category term="Particle Filter" /><category term="SMC" /><category term="Sequential Monte Carlo" /><category term="Importance Sampling" /><category term="Filtering" /><summary type="html"><![CDATA[이 포스팅은 Sequential Monte Carlo(SMC) 방법의 대표 알고리즘인 Particle Filter 의 핵심 개념과 구조를 정리한 글입니다.]]></summary></entry><entry><title type="html">Filtering &amp;amp; State Space Model</title><link href="https://joon0390.github.io/filtering/" rel="alternate" type="text/html" title="Filtering &amp;amp; State Space Model" /><published>2025-11-12T00:00:00+09:00</published><updated>2025-11-12T00:00:00+09:00</updated><id>https://joon0390.github.io/Filtering</id><content type="html" xml:base="https://joon0390.github.io/filtering/"><![CDATA[<blockquote>
  <p>이 포스팅은 동적 시스템(Dynamic System)에서의 <strong>필터링(Filtering)</strong> 과 <strong>상태공간모형(State-Space Model)</strong> 에 대한 기본 개념을 정리한 글입니다.</p>
</blockquote>

<h2 id="introduction">Introduction</h2>

<p>현실 세계의 많은 문제는 <strong>시간에 따라 변화하는 시스템</strong>을 다루고 있으며, 이 시스템의 내부 상태는 직접적으로 관찰하기 어려운 경우가 많습니다.</p>

<p>예를 들어,</p>

<ul>
  <li>GPS가 끊길 때 로봇의 <strong>실제 위치</strong>는 관측되지 않지만 지속적으로 추정해야 합니다.</li>
  <li>센서 노이즈로 인해 <strong>실제 신호 값</strong>을 직접적으로 보기 어렵습니다.</li>
  <li>금융 시계열에서는 시장의 <strong>숨겨진 변동성(latent volatility)</strong> 을 추정하는 것이 핵심입니다.</li>
</ul>

<p>이처럼 <strong>관찰할 수 없는 상태(hidden state)를 관측 가능한 데이터로부터 추정</strong>하는 문제가 바로 <strong>필터링(filtering)</strong> 입니다.</p>

<h2 id="필터링filtering이란-무엇인가">필터링(Filtering)이란 무엇인가?</h2>

<p>필터링은 다음 분포를 추정하는 문제입니다:</p>

\[p(x_t \mid y_{1:t})\]

<p>여기서</p>

<ul>
  <li>$x_t$: 현재 시간의 숨겨진 상태(hidden state)</li>
  <li>$y_{1:t}$: 과거부터 현재까지의 누적된 관측 데이터</li>
</ul>

<p>즉, 지금까지 들어온 관측치들을 바탕으로 <strong>현재의 은닉 상태가 어떤 값일지</strong> 확률적으로 추정하는 것이 필터링입니다.</p>

<h3 id="-filtering-prediction-smoothing의-차이">✔ Filtering, Prediction, Smoothing의 차이</h3>

<table>
  <thead>
    <tr>
      <th>문제</th>
      <th>목표</th>
      <th>조건부 분포</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Prediction</strong></td>
      <td>미래 상태 예측</td>
      <td>$ p(x_{t+1} \mid y_{1:t}) $</td>
    </tr>
    <tr>
      <td><strong>Filtering</strong></td>
      <td>현재 상태 추정</td>
      <td>$ p(x_t \mid y_{1:t}) $</td>
    </tr>
    <tr>
      <td><strong>Smoothing</strong></td>
      <td>과거 상태 재추정</td>
      <td>$ p(x_t \mid y_{1:T}) $</td>
    </tr>
  </tbody>
</table>

<p>필터링은 “<strong>online</strong>” 문제이며, smoothing은 “<strong>offline</strong>” 문제입니다.</p>

<h2 id="state-space-model-ssm">State-Space Model (SSM)</h2>

<p>필터링을 정의하려면, 시스템이 어떻게 변화하고 관측되는지 모델링해야 합니다.<br />
가장 일반적인 틀이 바로 <strong>상태공간모형(State-Space Model)</strong> 입니다.</p>

<p>SSM은 두 부분으로 구성됩니다.</p>

<h3 id="1-상태전이모형-state-transition-model">1) 상태전이모형 (State Transition Model)</h3>

\[x_t \sim p(x_t \mid x_{t-1})\]

<ul>
  <li>시스템이 <strong>시간에 따라 어떻게 변화</strong>하는지 나타냄</li>
  <li>ex: 로봇의 위치 변화, 금융 시장의 변동성 변화 등</li>
</ul>

<h3 id="2-관측모형-observation-model">2) 관측모형 (Observation Model)</h3>

\[y_t \sim p(y_t \mid x_t)\]

<ul>
  <li>은닉 상태 $x_t$가 어떻게 관측치 $y_t$를 만들어내는지 표현</li>
  <li>ex: 센서 노이즈 모델, 측정 에러 등</li>
</ul>

<h3 id="ssm의-직관적-이해">SSM의 직관적 이해</h3>

<ul>
  <li><strong>진짜 세계가 $x_t$</strong> (하지만 우리는 못 본다)</li>
  <li><strong>관측되는 데이터는 $y_t$</strong> (노이즈 포함)</li>
  <li>우리는 $y_t$로부터 다시 $x_t$를 역추론해야 한다</li>
</ul>

<p>필터링이 어떻게 이루어지는지 이해하기 위해 필요한 개념은 단 두 가지입니다.</p>

<h3 id="-1-prediction-과거-posterior를-이용해-현재-prior-생성">✔ 1) Prediction (과거 posterior를 이용해 현재 prior 생성)</h3>

\[p(x_t \mid y_{1:t-1})
=
\int p(x_t \mid x_{t-1})
\ p(x_{t-1} \mid y_{1:t-1})
\ dx_{t-1}\]

<h3 id="-2-update-관측을-반영해-posterior-생성">✔ 2) Update (관측을 반영해 posterior 생성)</h3>

\[p(x_t \mid y_{1:t})
\propto
p(y_t \mid x_t)\,
p(x_t \mid y_{1:t-1})\]

<p>이 두 식이 <strong>모든 필터링 알고리즘의 기반</strong>입니다.<br />
이 식을 <strong>정확하게 계산할 수 있느냐 없느냐</strong>에 따라 알고리즘이 크게 달라집니다.</p>

<h2 id="해석적analytic-풀이가-가능한-경우-kalman-filter">해석적(analytic) 풀이가 가능한 경우: Kalman Filter</h2>

<p>특수한 경우에 위 식들이 정확히(Closed-form solution이 존재) 계산 가능합니다.</p>

<h3 id="-선형-transition--관측">✔ 선형 transition + 관측</h3>

\[x_t = A x_{t-1} + \epsilon_t,\quad
y_t = C x_t + \eta_t\]

<h3 id="-gaussian-잡음">✔ Gaussian 잡음</h3>

\[\epsilon_t \sim \mathcal{N}(0,Q),
\quad
\eta_t \sim \mathcal{N}(0,R)\]

<h3 id="그러면-무슨-일이-생기나">그러면 무슨 일이 생기나?</h3>

<ul>
  <li>posterior가 항상 Gaussian 형태 유지</li>
  <li>평균·분산만 업데이트하면 됨</li>
  <li>필터링을 <strong>closed-form</strong>으로 계산 가능</li>
</ul>

<p>그래서 Kalman Filter는</p>

<ul>
  <li>항공/우주 항법</li>
  <li>자율주행</li>
  <li>센서 융합(핸드폰 IMU, GPS)</li>
  <li>공학 제어 시스템</li>
</ul>

<p>등에서 주로 사용됩니다.</p>

<h2 id="하지만-현실-시스템은-대부분-더-복잡하다">하지만 현실 시스템은 대부분 더 복잡하다</h2>

<p>실제 문제는 대체로:</p>

<ul>
  <li>Nonlinear</li>
  <li>Non-Gaussian</li>
  <li>multi-modal</li>
  <li>Contain outlier</li>
</ul>

<p>example:</p>

<ul>
  <li>로봇 이동에 삼각함수 포함</li>
  <li>금융 노이즈는 heavy-tailed</li>
  <li>뇌 latent dynamics는 복잡한 비선형 구조</li>
  <li>센서의 이상치는 가우시안 가정을 깨뜨림</li>
</ul>

<p>이 경우 Kalman Filter는 더 이상 정확한 추정을 보장할 수 없습니다.</p>

<h2 id="ekf와-ukf-비선형-근사-접근">EKF와 UKF: 비선형 근사 접근</h2>

<p>비선형 시스템을 다루기 위해 다음 두 확장판이 도입되었습니다.</p>

<h3 id="1-extended-kalman-filter-ekf">1) Extended Kalman Filter (EKF)</h3>

<ul>
  <li>비선형 함수 Transition과 Observation $f, g$ 를 Jacobian을 이용하여 일차 테일러 근사를 통해 모델을 강제로 선형 근사합니다.</li>
  <li>비교적 간단하지만, 강하게 비선형이면 부정확할 수 있습니다.</li>
</ul>

<h3 id="2-unscented-kalman-filter-ukf">2) Unscented Kalman Filter (UKF)</h3>

<ul>
  <li>$f,g$를 선형화하는 것 대신, 분포를 선형화합니다.
    <ul>
      <li>Unscented Transform(UT) 방법을사용하여 posterior 분포를 대표하는 sigma points를 생성하고 이 포인트들을 비선형 함수에 통과시켜 새로운 분포를 얻습니다.</li>
    </ul>
  </li>
  <li>EKF보다 안정적이며, 비선형에 더 강하고, 2차 정확도 수준의 근사 품질을 갖지만,</li>
  <li><strong>여전히 Gaussian 형태의 posterior를 가정</strong>합니다.</li>
</ul>

<h2 id="자연스러운-결론-더-일반화된-필터가-필요하다">자연스러운 결론: 더 일반화된 필터가 필요하다</h2>

<p>여기까지 요약하면,</p>

<ul>
  <li>Kalman 계열은 특정 상황(선형+정규)에선 매우 강력</li>
  <li>하지만 현실 데이터가 복잡하면 가정이 쉽게 깨짐</li>
  <li>그 결과 <strong>posterior가 어떤 모양이든 유연하게 근사할 수 있는 방법</strong>이 필요하다.</li>
</ul>

<p>그것 중 하나가 <strong>Particle Filter (Sequential Monte Carlo)</strong> 입니다.</p>

<h1 id="다음-편-particle-filter-sequential-monte-carlo">다음 편: Particle Filter (Sequential Monte Carlo)</h1>

<p>다음 글에서는 다음 내용을 다룰 예정입니다.</p>

<ul>
  <li>Particle Filter의 기본 아이디어</li>
  <li>SIR (Sampling–Importance–Resampling) 알고리즘</li>
  <li>Degeneracy &amp; ESS</li>
  <li>Auxiliary Particle Filter</li>
  <li>Particle Learning</li>
  <li>SMC²</li>
</ul>

<p>그리고 필터링의 Prediction–Update 구조가<br />
어떻게 <strong>Monte Carlo 기반의 샘플 집합(particles)</strong> 으로 구현되는지 설명합니다.</p>

<h2 id="reference">Reference</h2>

<ul>
  <li>Sarkka, S. (2013). <em>Bayesian Filtering and Smoothing</em>.</li>
  <li>Doucet, de Freitas, Gordon (2001). <em>Sequential Monte Carlo Methods in Practice</em>.</li>
  <li>Ristic et al. (2004). <em>Beyond the Kalman Filter</em>.</li>
  <li>Kevin P. Murphy (2023). <em>Probabilistic Machine Learning</em>.</li>
</ul>]]></content><author><name>Junhee Kim</name></author><category term="Statistics" /><category term="Bayesian" /><category term="Filtering" /><category term="State Space Model" /><category term="Bayesian Filtering" /><category term="Kalman Filter" /><summary type="html"><![CDATA[이 포스팅은 동적 시스템(Dynamic System)에서의 필터링(Filtering) 과 상태공간모형(State-Space Model) 에 대한 기본 개념을 정리한 글입니다.]]></summary></entry></feed>